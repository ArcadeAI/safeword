{
  "feature": "features/portable-tracker-transport.feature",
  "scenarios": {
    "A never-synced ticket becomes a create intent": [
      "packages/cli/tests/tracker-sync/plan.test.ts",
      "emits a create intent with the minimal payload for a never-synced ticket"
    ],
    "An already-recorded ticket becomes an update intent": [
      "packages/cli/tests/tracker-sync/plan.test.ts",
      "emits an update intent carrying the recorded ref for a recorded open ticket"
    ],
    "A terminal ticket becomes a close intent": [
      "packages/cli/tests/tracker-sync/plan.test.ts",
      "emits a close intent with the ref and full (closed) payload for a recorded terminal ticket"
    ],
    "A never-synced ticket that is already terminal becomes a create carrying a closed state": [
      "packages/cli/tests/tracker-sync/plan.test.ts",
      "emits a create intent with a closed payload for a terminal never-synced ticket"
    ],
    "An empty corpus yields an empty but valid plan": [
      "packages/cli/tests/tracker-sync/plan.test.ts",
      "emits an empty but versioned plan for an empty corpus"
    ],
    "A ticket with a parent carries a parent edge": [
      "packages/cli/tests/tracker-sync/plan.test.ts",
      "carries a parent edge naming the parent ticket id"
    ],
    "A blocked ticket carries its blocked-by edges as a set": [
      "packages/cli/tests/tracker-sync/plan.test.ts",
      "carries blocked-by edges as an unordered set of ticket ids"
    ],
    "A ticket with both a parent and a blocked-by edge carries both": [
      "packages/cli/tests/tracker-sync/plan.test.ts",
      "carries both a parent and a blocked-by edge"
    ],
    "Only the unresolvable edge is dropped; resolvable edges remain": [
      "packages/cli/tests/tracker-sync/plan.test.ts",
      "drops only the unresolvable edge, keeping resolvable ones"
    ],
    "Planning needs no credential and contacts no tracker": [
      "packages/cli/tests/tracker-sync/command-plan-apply.test.ts",
      "--plan writes a single valid SyncPlan JSON document to stdout and nothing else"
    ],
    "A create result is recorded with its issue number and url": [
      "packages/cli/tests/tracker-sync/apply-results.test.ts",
      "records a create result with the bare number and url as recorded"
    ],
    "Re-applying the same results changes nothing": [
      "packages/cli/tests/tracker-sync/apply-results.test.ts",
      "is idempotent — re-applying the same results changes nothing"
    ],
    "An update or close result makes no identity change": [
      "packages/cli/tests/tracker-sync/apply-results.test.ts",
      "leaves an already-recorded ref unchanged for an update/close ack"
    ],
    "A malformed results file is rejected and the map is left intact": [
      ["packages/cli/tests/tracker-sync/apply-results.test.ts", "rejects invalid JSON"],
      [
        "packages/cli/tests/tracker-sync/command-plan-apply.test.ts",
        "rejects a missing results file without writing the sidecar"
      ],
      [
        "packages/cli/tests/tracker-sync/apply-results.test.ts",
        "rejects an unsupported contract version"
      ],
      [
        "packages/cli/tests/tracker-sync/apply-results.test.ts",
        "rejects a create result missing its number"
      ],
      [
        "packages/cli/tests/tracker-sync/apply-results.test.ts",
        "rejects a create result missing its url"
      ],
      [
        "packages/cli/tests/tracker-sync/apply-results.test.ts",
        "rejects a result whose url tail does not match its number, leaving the map untouched"
      ],
      [
        "packages/cli/tests/tracker-sync/apply-results.test.ts",
        "rejects a result naming a ticket absent from the corpus, leaving the map untouched"
      ]
    ],
    "Applying against a corrupt tracker map is refused, leaving the file intact": [
      "packages/cli/tests/tracker-sync/command-plan-apply.test.ts",
      "refuses to apply against a corrupt sidecar, leaving it untouched"
    ],
    "A planned create round-trips through results back into the map": [
      "packages/cli/tests/tracker-sync/apply-results.test.ts",
      "a planned create, executed and applied, records the ticket in the map"
    ],
    "--plan writes a valid SyncPlan to stdout and nothing else": [
      "packages/cli/tests/tracker-sync/command-plan-apply.test.ts",
      "--plan writes a single valid SyncPlan JSON document to stdout and nothing else"
    ],
    "Planning an unconfigured project yields an empty plan, not a plan full of creates": [
      "packages/cli/tests/tracker-sync/command-plan-apply.test.ts",
      "--plan on an unconfigured project emits an empty plan, not a plan full of creates"
    ],
    "Planning full ticket bodies to GitHub warns about egress without polluting the plan": [
      "packages/cli/tests/tracker-sync/command-plan-apply.test.ts",
      "--plan warns on stderr when planning full bodies to github, keeping stdout pure"
    ],
    "With no mode flag, the command routes to the gh path": [
      "packages/cli/tests/tracker-sync/wiring.test.ts",
      "projects a real ticket using the GitHub CLI keychain credential"
    ],
    "Plan and apply modes cannot be combined": [
      "packages/cli/tests/tracker-sync/command-plan-apply.test.ts",
      "rejects combining --plan and --apply-results"
    ],
    "A create intent body carries only minimal egress": [
      "packages/cli/tests/tracker-sync/plan.test.ts",
      "emits a create intent with the minimal payload for a never-synced ticket"
    ],
    "The emitted plan contains no credential": [
      "packages/cli/tests/tracker-sync/command-plan-apply.test.ts",
      "never emits a credential in the plan, even when one is in the environment"
    ]
  }
}
