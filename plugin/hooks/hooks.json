{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "test -d \"${CLAUDE_PROJECT_DIR:-.}/.safeword\" || echo 'SAFEWORD: Not yet configured. Run `bunx safeword@latest setup` to install (auto-detects languages, no prompts). If bunx is unavailable, use `npx safeword@latest setup`. Report what was installed.'"
          }
        ]
      }
    ]
  }
}
