{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "test -d \"${CLAUDE_PROJECT_DIR:-.}/.safeword\" || echo 'SAFEWORD: Not configured for this project. Run /safeword:setup to install AI coding workflows (BDD, auto-linting, quality reviews, and more).'"
          }
        ]
      }
    ]
  }
}
