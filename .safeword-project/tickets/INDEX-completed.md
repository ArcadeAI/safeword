# Project Tickets — Completed Archive

<!-- Auto-generated by `safeword sync-tickets`. Do not edit by hand. -->
<!-- Completed tickets (the completed/ archive), grouped by epic. -->

## Tickets (158)

### bdd-phase-zero-merge

- **7YN5QB** — Add persona model (.safeword-project/personas.md) + Phase 0 validation (done, epic: bdd-phase-zero-merge)
  Introduce a project-wide personas file as the source of truth for who features serve, and validate persona references in `bdd` Phase 0 against it.
  → `.safeword-project/tickets/completed/7YN5QB`

### setup-lifecycle

- **074** — LOC gate should exclude safeword/tooling files from line count (done, epic: setup-lifecycle)
  → `.safeword-project/tickets/completed/074-loc-gate-exclude-tooling`
- **081** — Auto-upgrade safeword on session start (done, epic: setup-lifecycle)
  → `.safeword-project/tickets/completed/081-auto-upgrade`

### (no epic)

- **001** — Stateful BDD Flow (done, epic: —)
  Agent knows where it is in BDD workflow and acts accordingly.
  → `.safeword-project/tickets/completed/001-stateful-bdd-flow`
- **002** — Single Source of Truth for Cross-Platform Skills (cancelled, epic: —)
  Eliminate manual duplication between Claude skills and Cursor rules.
  → `.safeword-project/tickets/completed/002-cross-platform-skill-source`
- **006** — Phase-Aware Quality Review (done, epic: —)
  Quality review hook provides context-appropriate prompts based on current BDD phase.
  → `.safeword-project/tickets/completed/006-phase-aware-quality`
- **007** — Iteration 6: Implementation Phases (5-7) (done, epic: —)
  Complete BDD workflow with robust implementation phases - technical decomposition, outside-in TDD guidance, and done gate with comprehensive audit.
  → `.safeword-project/tickets/completed/007-iteration6-implementation-phases`
- **008** — Ticket/Artifact Folder Reorganization (done, epic: —)
  Consolidate all project artifacts into `.safeword-project/` with ticket folders.
  → `.safeword-project/tickets/completed/008-ticket-folder-reorganization`
- **010** — Iteration 7: Decomposition at Checkpoints (done, epic: —)
  Agent suggests splitting large work at 5 checkpoints with calibrated thresholds.
  → `.safeword-project/tickets/completed/010-iteration7-decomposition`
- **013** — Restructure skills for LLM optimization (done, epic: —)
  → `.safeword-project/tickets/completed/013-skills-restructuring`
- **013a** — BDD skill compression and phase-based splitting (done, epic: —)
  → `.safeword-project/tickets/completed/013a-bdd-skill-compression`
- **013b** — Phase-aware quality review skill with web research (done, epic: —)
  → `.safeword-project/tickets/completed/013b-quality-review-restructure`
- **013c** — Consolidate TDD content between testing guide and BDD skill (done, epic: —)
  → `.safeword-project/tickets/completed/013c-testing-guide-consolidation`
- **013d** — BDD Flow Enforcement Improvements (done, epic: —)
  Ensure LLMs maintain ticket state and work logs throughout BDD workflow execution.
  → `.safeword-project/tickets/completed/013d-bdd-flow-enforcement`
- **013e** — Schema File Validation Command (done, epic: —)
  Add a test that verifies schema ↔ template file integrity.
  → `.safeword-project/tickets/completed/013e-schema-file-validation`
- **014** — BDD Skill vs Guides: Duplication & Misalignment Report (done, epic: —)
  → `.safeword-project/tickets/completed/014-bdd-guides-consolidation`
- **015** — Format and lint on save for safeword contributors (done, epic: —)
  Add `.vscode/settings.json` to the safeword monorepo for developer experience.
  → `.safeword-project/tickets/completed/015-cursor-ide-optimization`
- **016** — IDE and Claude Code Integration (done, epic: —)
  Provide seamless IDE integration and Claude Code safeguards for both safeword developers and customers.
  → `.safeword-project/tickets/completed/016-ide-developer-experience`
- **016b** — Format and lint on save for customer projects (cancelled, epic: —)
  Add `.vscode/settings.json` to customer projects via `safeword setup`.
  → `.safeword-project/tickets/completed/016b-customer-vscode-template`
- **017** — Continuous Quality Monitoring for Long-Running Agent Sessions (superseded, epic: —)
  Ensure quality checks happen at appropriate intervals during long BDD runs, not just when the agent stops for user input.
  → `.safeword-project/tickets/completed/017-continuous-quality-monitoring`
- **017a** — LOC-Based Commit Enforcement (superseded, epic: —)
  Block edits when 400+ LOC uncommitted, enforcing TDD commit discipline.
  → `.safeword-project/tickets/completed/017a-loc-quality-tracking`
- **017b** — Phase Transition Quality Gates (superseded, epic: —)
  Trigger quality review at BDD phase boundaries, ensuring work is validated before moving to the next phase.
  → `.safeword-project/tickets/completed/017b-phase-transition-gates`
- **018** — IDE Parity: Eliminate Claude Code / Cursor Configuration Drift (cancelled, epic: —)
  Single source of truth for Safeword configuration that works in both IDEs.
  → `.safeword-project/tickets/completed/018-ide-parity`
- **018a** — Unified Hook I/O Library (cancelled, epic: —)
  Create `lib/io.ts` that normalizes input/output across IDEs, eliminating `.safeword/hooks/cursor/`.
  → `.safeword-project/tickets/completed/018a-unified-hook-io`
- **018b** — Skill Sync from Single Source (cancelled, epic: —)
  Single source of truth for skill content, with the build/reconcile process generating IDE-specific formats.
  → `.safeword-project/tickets/completed/018b-skill-sync`
- **018c** — Command Sync (cancelled, epic: —)
  Single source for commands in `.safeword/commands/`, copied to both IDE directories.
  → `.safeword-project/tickets/completed/018c-command-sync`
- **020** — Document user-focused ticket naming conventions (done, epic: —)
  Add guidance on writing user-focused ticket names to AGENTS.md or a planning guide.
  → `.safeword-project/tickets/completed/020-ticket-naming-guide`
- **021** — Smart Quality Review (Prompt-Based) (wontfix, epic: —)
  Use LLM evaluation for deeper quality analysis beyond simple thresholds.
  → `.safeword-project/tickets/completed/021-smart-quality-review`
- **022** — Hierarchical Work State Machine (superseded, epic: —)
  Unified state machine that tracks nested work items, injects phase-appropriate context, and enforces commits at natural checkpoints.
  → `.safeword-project/tickets/completed/022-hierarchical-work-state`
- **023** — Enable Rust developers to use safeword's quality workflow (complete, epic: —)
  Add Rust language support so Rust projects get automatic linting and formatting with strict defaults.
  → `.safeword-project/tickets/completed/023-rust-language-pack`
- **024** — Continuous Quality Gates (LOC + Phase) (done, epic: —)
  Block edits when 400+ LOC uncommitted (commit discipline) and at phase transitions (process compliance), injecting phase-appropriate context at each gate.
  → `.safeword-project/tickets/completed/024-continuous-quality-gates`
- **025** — Hierarchy Navigation After Ticket Completion (done, epic: —)
  After completing a child ticket, the Stop hook walks the ticket hierarchy to find the next undone sibling and directs the agent to start it. When all siblings are done, cascade completion up to the parent.
  → `.safeword-project/tickets/completed/025-hierarchy-navigation`
- **026** — Extract shared vitest base config (done, epic: —)
  Eliminate ~75% duplication between vitest.config.ts and vitest.slow.config.ts by extracting shared settings into a base config.
  → `.safeword-project/tickets/completed/026-vitest-config-dedup`
- **027** — Consolidate package manager command mapping (done, epic: —)
  Replace 3 functions with identical Record<PackageManager, ...> patterns with a single data structure.
  → `.safeword-project/tickets/completed/027-install-command-dedup`
- **028** — Merge duplicate Rust workspace test helpers (done, epic: —)
  Merge createRustWorkspace and createRustWorkspaceWithGlob into a single function with a parameter for member format.
  → `.safeword-project/tickets/completed/028-rust-workspace-dedup`
- **029** — Distribute Quality Gate Hooks to Consumer Projects (done, epic: —)
  Ship all 4 quality enforcement hooks (`pre-tool-quality.ts`, `post-tool-quality.ts`, `pre-tool-config-guard.ts`, `post-tool-bypass-warn.ts`) + shared lib (`lib/quality-state.ts`) via `safeword setup` and `safeword upgrade` so consumer projects get the same TDD refactor gate, LOC gate, phase gate, config protection, and bypass warnings that the dogfood repo has.
  → `.safeword-project/tickets/completed/029-distribute-quality-hooks`
- **030** — Rename /done to /verify with Dependency Drift Check (done, epic: —)
  Replace the `/done` command with `/verify` — a self-documenting verb that proves a ticket meets its criteria. Add dependency drift detection (package.json vs ARCHITECTURE.md) to `/verify`. Require `/audit` evidence in the stop hook for done phase completion.
  → `.safeword-project/tickets/completed/030-rename-done-to-verify`
- **032** — Re-inject ticket context after compaction to prevent context loss (done, epic: —)
  Add a PostCompact hook that re-injects active ticket context, current BDD phase, and quality state after Claude Code compacts the conversation.
  → `.safeword-project/tickets/completed/032-post-compact-context-reinject`
- **033** — Guard stop-quality hook against infinite loops with stop_hook_active (done, epic: —)
  Add `stop_hook_active` check to the top of stop-quality.ts to deterministically prevent infinite Stop hook loops.
  → `.safeword-project/tickets/completed/033-stop-hook-loop-guard`
- **034** — Add missing skill frontmatter fields for invocation control (done, epic: —)
  Add `user-invocable: false` to the `testing` skill (reference knowledge, not an action) and evaluate `disable-model-invocation` for action commands once the --resume bug is fixed.
  → `.safeword-project/tickets/completed/034-skill-frontmatter-fields`
- **035** — Deduplicate overlapping content across SAFEWORD.md, AGENTS.md, and CLAUDE.md (done, epic: —)
  Eliminate content duplication between the three context files to reduce token waste and prevent drift.
  → `.safeword-project/tickets/completed/035-deduplicate-context-files`
- **038** — Convert 4 standalone commands to skills with disable-model-invocation (done, epic: —)
  Convert lint, verify, audit, and cleanup-zombies from `.claude/commands/` to `.claude/skills/` with `disable-model-invocation: true` to prevent Claude from auto-triggering them.
  → `.safeword-project/tickets/completed/038-convert-commands-to-skills`
- **039** — Remove JSON response format, trigger quality review on edit tools only (done, epic: —)
  Replace the self-reporting JSON format (`proposedChanges`/`madeChanges`/`askedQuestion`) with direct edit-tool detection. Quality reviews only fire when Claude actually edited files.
  → `.safeword-project/tickets/completed/039-remove-json-response-format`
- **040** — Add dbt language pack for SQL linting and formatting (done, epic: —)
  Let safeword lint and auto-fix `.sql` files on edit in dbt projects, using SQLFluff with Jinja-aware parsing.
  → `.safeword-project/tickets/completed/040-sql-dbt-language-pack`
- **041** — TDD Inner-Loop Quality Gates (done, epic: —)
  Add quality review gates at each TDD sub-phase boundary (RED, GREEN, REFACTOR) so that work is reviewed before transitioning to the next step.
  → `.safeword-project/tickets/completed/041-tdd-quality-gates`
- **043** — Quality Gate Deadlock: Pre-tool blocks edits to project artifacts (done, epic: —)
  Fix circular dependency where quality gates block edits to `.safeword-project/` files, creating unrecoverable deadlocks.
  → `.safeword-project/tickets/completed/043-quality-gate-deadlock-fix`
- **044** — Quality Gate Robustness (done, epic: —)
  Fix quality gate failure modes discovered in production use — deadlocks on project artifacts and cross-session state contamination.
  → `.safeword-project/tickets/completed/044-quality-gate-robustness`
- **045** — Per-Session Quality State (done, epic: —)
  Isolate quality gate state per Claude session to prevent cross-session interference.
  → `.safeword-project/tickets/completed/045-per-session-quality-state`
- **046** — Phase-Based Access Control in Pre-Tool Hook (done, epic: —)
  Restrict file edits based on ticket phase — planning phases allow only `.safeword-project/` edits, implement allows everything.
  → `.safeword-project/tickets/completed/046-phase-access-control`
- **048** — Switch done-phase hard block from exit(2) to JSON decision:block (done, epic: —)
  Replace `hardBlockDone`'s `console.error + process.exit(2)` with the canonical `{ decision: 'block', reason } + process.exit(0)` pattern in `stop-quality.ts`.
  → `.safeword-project/tickets/completed/048-done-phase-hardblock-json`
- **049** — Stop Hook Quality Gate Improvements (done, epic: —)
  Progressively strengthen the stop hook system based on research into Claude Code hook mechanics, LLM self-correction literature, and Goodhart's Law failure modes.
  → `.safeword-project/tickets/completed/049-stop-hook-quality-improvements`
- **049a** — Return `suppressOutput: true` from silent observer hooks (blocked, epic: —)
  Add `suppressOutput: true` to the JSON output of silent observer hooks (PostToolUse quality, SessionEnd cleanup) to reduce false "hook error" noise injected into Claude's context.
  → `.safeword-project/tickets/completed/049a-suppressoutput-silent-hooks`
- **049b** — Reframe soft block comments as "quality prompt" not "gate" (done, epic: —)
  Update comments in `stop-quality.ts` so the soft block is accurately described as a quality review prompt (not a gate), reflecting that its purpose is to leverage Claude's judgment on things external tools cannot check.
  → `.safeword-project/tickets/completed/049b-reframe-softblock-comments`
- **049c** — Scope done-phase evidence matching to Bash tool output (done, epic: —)
  Change done-phase evidence detection in `stop-quality.ts` to match patterns only within actual Bash tool result blocks in the transcript, rather than anywhere in Claude's last message text.
  → `.safeword-project/tickets/completed/049c-scope-evidence-to-bash-output`
- **049d** — Hook runs tests directly and gates on exit code (done, epic: —)
  For done-phase tickets, have the stop hook directly execute the test suite and gate on its exit code — rather than relying on Claude reporting that tests passed.
  → `.safeword-project/tickets/completed/049d-hook-runs-tests-directly`
- **049e** — Gate on test-definitions.md content, not just presence (done, epic: —)
  Strengthen the cumulative artifact check so that `test-definitions.md` must contain at least one real scenario, not just exist as an empty or stub file.
  → `.safeword-project/tickets/completed/049e-gate-testdefs-content`
- **049f** — Haiku as judge for soft block quality review (cancelled, epic: —)
  Replace the soft block's self-review prompt with an independent evaluator model (claude-haiku-4-5) that judges Claude's response for rationalization patterns and quality gaps before allowing it to stop.
  → `.safeword-project/tickets/completed/049f-haiku-judge-soft-block`
- **050** — Haiku as independent judge for stop hook soft block (wontfix, epic: —)
  Replace the soft block's self-review prompt with a Haiku API call that independently judges Claude's response for rationalization patterns and quality gaps.
  → `.safeword-project/tickets/completed/050-haiku-judge-stop-hook`
- **051** — Upgrade ESLint 9 → 10 (superseded, epic: —)
  Upgrade `eslint` from 9.39.4 to 10.x.
  → `.safeword-project/tickets/completed/051-upgrade-eslint-10`
- **052** — Upgrade knip 5 → 6 (done, epic: —)
  Upgrade `knip` from 5.88.1 to 6.x.
  → `.safeword-project/tickets/completed/052-upgrade-knip-6`
- **053** — Formally verify the phase/gate state machine (cancelled, epic: —)
  Model safeword's phase FSM, TDD cycle, and gate logic in a formal checker (Alloy or TLA+) and exhaustively verify correctness properties.
  → `.safeword-project/tickets/completed/053-formal-verify-state-machine`
- **055** — Pre-execution plan verification for agent workflows (cancelled, epic: —)
  Before an agent executes a multi-step workflow (BDD, TDD), verify the plan against declared constraints and reject invalid plans before work begins.
  → `.safeword-project/tickets/completed/055-plan-verification`
- **056** — SQL dialect auto-detection for the dbt/SQL language pack (done, epic: —)
  Automatically detect the correct SQLFluff dialect from project signals so that SQL linting works out of the box — zero config from the user.
  → `.safeword-project/tickets/completed/056-sql-dialect-auto-detection`
- **057** — Rename dbt pack → SQL pack and broaden activation (done, epic: —)
  Expand the dbt language pack into a general-purpose SQL pack that activates for any project with intentional SQL work — dbt, Flyway, sqlc, Atlas, Prisma migrations, and more.
  → `.safeword-project/tickets/completed/057-sql-language-pack`
- **058** — Unify language detection: single source of truth for pack detection (done, epic: —)
  Eliminate the dual detection paths that have already caused a real bug (Python `requirements.txt` not detected by pack registration) and prevent future drift as we add the SQL pack.
  → `.safeword-project/tickets/completed/058-unify-language-detection`
- **059** — Deep language detection: recursive scanning for monorepo marker files (done, epic: —)
  → `.safeword-project/tickets/completed/059-deep-language-detection`
- **060** — Task: Fix golangci-lint config to not override customer settings (done, epic: —)
  → `.safeword-project/tickets/completed/060-golangci-lint-additive-config`
- **061** — Task: Surface unfixable lint errors back to Claude via additionalContext (done, epic: —)
  → `.safeword-project/tickets/completed/061-lint-hook-feedback`
- **062** — Task: Rust clippy.toml thresholds override customer values (done, epic: —)
  → `.safeword-project/tickets/completed/062-rust-clippy-threshold-override`
- **063** — Task: Verify sqlfluff config layering doesn't override customer rules (done, epic: —)
  → `.safeword-project/tickets/completed/063-sqlfluff-config-layering`
- **064** — Separate dogfood parity check from main test suite (done, epic: —)
  Move the dogfood parity test out of `vitest run` so the main suite stays green during template iteration.
  → `.safeword-project/tickets/completed/064-dogfood-parity-separation`
- **066** — Task: Evaluate TypeScript 6 migration (superseded, epic: —)
  → `.safeword-project/tickets/completed/066-typescript-6-migration`
- **069** — Task: Verify CLI works with commander 14 (done, epic: —)
  → `.safeword-project/tickets/completed/069-commander-14-verification`
- **070** — Task: Publish safeword 0.27.0 (done, epic: —)
  → `.safeword-project/tickets/completed/070-publish-0-27`
- **071** — Task: Save "adds, never replaces" principle to project memory (done, epic: —)
  → `.safeword-project/tickets/completed/071-additive-config-memory`
- **073** — Task: Replace manual deploy steps with withastro/action (done, epic: —)
  → `.safeword-project/tickets/completed/073-deploy-withastro-action`
- **075** — Linter hook: distinguish crash from clean pass, surface stderr (done, epic: —)
  → `.safeword-project/tickets/completed/075-linter-resilience`
- **076** — Task: CI hardening — concurrency, timeouts, caching, golangci-lint binary (done, epic: —)
  → `.safeword-project/tickets/completed/076-ci-hardening`
- **077** — Task: Fix TypeScript errors from SQL pack additions (done, epic: —)
  → `.safeword-project/tickets/completed/077-fix-typescript-errors`
- **083** — Bump @types/node from 20.x to 25.x (done, epic: —)
  → `.safeword-project/tickets/completed/083-types-node-25`
- **084** — Fix 8 no-op test assertions that can never fail (done, epic: —)
  → `.safeword-project/tickets/completed/084-weak-test-assertions`
- **086** — Suppression Scope Enforcement Across All Checkers (done, epic: —)
  Enforce narrowest-scope suppressions across all code quality tools, not just ESLint.
  → `.safeword-project/tickets/completed/086-suppression-scope-enforcement`
- **087** — ESLint 9→10 Migration (superseded, epic: —)
  Upgrade ESLint from v9 to v10 across the preset and dev tooling.
  → `.safeword-project/tickets/completed/087-eslint-10-migration`
- **088** — Upgrade eslint-plugin-security 3→4 (done, epic: —)
  Upgrade eslint-plugin-security from v3.0.1 to v4.0.0.
  → `.safeword-project/tickets/completed/088-eslint-plugin-security-v4`
- **089** — Upgrade eslint-plugin-sonarjs 3→4 (done, epic: —)
  Upgrade eslint-plugin-sonarjs from v3.0.7 to v4.0.2.
  → `.safeword-project/tickets/completed/089-eslint-plugin-sonarjs-v4`
- **090** — Upgrade eslint-plugin-storybook 0.12→10 (done, epic: —)
  Upgrade eslint-plugin-storybook from v0.12.0 to v10.3.3.
  → `.safeword-project/tickets/completed/090-eslint-plugin-storybook-v10`
- **092** — Fix eslint-comments/disable-enable-pair errors in hierarchy tests (done, epic: —)
  → `.safeword-project/tickets/completed/092-eslint-comments-missing-enable`
- **093** — Remove unused sonarjs/code-eval disable directive (done, epic: —)
  → `.safeword-project/tickets/completed/093-unused-sonarjs-disable`
- **094** — Cross-Session Phase-Access Blocking (done, epic: —)
  Prevent stale `in_progress` tickets from other sessions from blocking code edits in the current session.
  → `.safeword-project/tickets/completed/094-cross-session-phase-blocking`
- **095** — Session State Lifecycle — Clear Done Tickets (done, epic: —)
  Automatically clear the session's `activeTicket` when a ticket reaches `done` phase, so the done-phase gate doesn't block subsequent non-ticket work in the same session.
  → `.safeword-project/tickets/completed/095-session-state-lifecycle`
- **096** — Schema Change → Test Drift Reminder (done, epic: —)
  When schema.ts is modified, remind the agent to run `/verify` before pushing to catch test drift.
  → `.safeword-project/tickets/completed/096-schema-change-test-reminder`
- **097** — Pre-Push Test Gate (done, epic: —)
  Add a git pre-push hook that runs the test suite before allowing push to main.
  → `.safeword-project/tickets/completed/097-pre-push-test-gate`
- **098** — Unify Active Ticket Resolution Model (done, epic: —)
  Eliminate the dual-model inconsistency where pre-tool uses session-scoped binding and stop hook uses global ticket scan.
  → `.safeword-project/tickets/completed/098-unify-active-ticket-model`
- **100** — Propose-and-converge interaction pattern (done, epic: —)
  → `.safeword-project/tickets/completed/100-ideation-phase`
- **101** — Apply propose-and-converge to Safeword hooks (done, epic: —)
  → `.safeword-project/tickets/completed/101-hooks-propose-converge`
- **105** — Agent reasoning discipline — explore-debate before proposing (done, epic: —)
  → `.safeword-project/tickets/completed/105-agent-reasoning-discipline`
- **109** — Enforcement redesign: Natural Gates, Reminders, and Output Validation (done, epic: —)
  → `.safeword-project/tickets/completed/109-enforcement-redesign`
- **109** — Replace "best practices" with "latest research" in quality prompts (done, epic: —)
  → `.safeword-project/tickets/completed/109-replace-best-practices-wording`
- **111** — Reflexion-style structured failure memory for reminders (done, epic: —)
  → `.safeword-project/tickets/completed/111-reflexion-failure-memory`
- **112** — Audit and overhaul CLI guides (done, epic: —)
  Review all 10 guides for correctness, elegance, agent ergonomics, and duplication — ensure each earns its place and is effective at its stated intention.
  → `.safeword-project/tickets/completed/112-guides-overhaul`
- **112a** — Trim learning-extraction.md from 526 to ~150 lines (done, epic: —)
  Cut the guide to its core value — triggers, locations, decision tree, templates, anti-patterns — by removing the ~400 lines of repetition and self-evident instruction.
  → `.safeword-project/tickets/completed/112a-learning-extraction-trim`
- **112b** — Trim planning-guide.md from 417 to ~230 lines (done, epic: —)
  Cut Part 2 (Test Definitions, lines 226-417) which duplicates the test-definitions-feature template, testing-guide, and testing SKILL.md. Replace with a pointer. Keep Part 1 (User Stories) intact with minor example trimming.
  → `.safeword-project/tickets/completed/112b-planning-guide-trim`
- **112c** — Deduplicate testing-guide.md and testing SKILL.md (done, epic: —)
  Eliminate ~80% content overlap across 690 combined lines (409 guide + 280 skill). Separate concerns: skill = enforceable rules during workflow, guide = reference patterns and strategies.
  → `.safeword-project/tickets/completed/112c-testing-guide-dedupe`
- **112d** — Trim zombie-process-cleanup.md from 285 to ~180 lines (done, epic: —)
  Fix correctness issues, cut human-oriented content that doesn't serve the agent, remove repeated content.
  → `.safeword-project/tickets/completed/112d-zombie-cleanup-trim`
- **112e** — Trim architecture-guide.md from 392 to ~300 lines (done, epic: —)
  Cut restatements, add agent-visibility note, keep the layers & boundaries section (crown jewel) and decision tree intact.
  → `.safeword-project/tickets/completed/112e-architecture-guide-trim`
- **112f** — Trim context-files-guide.md from 454 to ~200 lines (done, epic: —)
  Remove content that restates Claude Code's official docs (~180 lines), fix outdated claims, add explicit pointers so the agent knows WHERE to find the offloaded content.
  → `.safeword-project/tickets/completed/112f-context-files-guide-trim`
- **112g** — Update llm-writing-guide.md with latest Anthropic research (done, epic: —)
  The most cross-referenced guide (6 inbound links) must be near-perfect. Update with verified 2026 Anthropic research, fix outdated claims, keep what's solid.
  → `.safeword-project/tickets/completed/112g-llm-writing-guide-update`
- **112h** — Remove cli-reference.md, inline decision table into SAFEWORD.md (done, epic: —)
  Eliminate a guide that duplicates `bunx safeword --help` and has zero inbound references.
  → `.safeword-project/tickets/completed/112h-cli-reference-remove`
- **113** — BDD skill file updates — reduce ceremony, increase agent judgment (done, epic: —)
  → `.safeword-project/tickets/completed/113-bdd-skill-file-updates`
- **114** — Enforcement architecture — natural gates + reminders, remove hard blocks (done, epic: —)
  → `.safeword-project/tickets/completed/114-enforcement-architecture`
- **115** — Stop hook improvements — structural scenario verification + simplified review (done, epic: —)
  → `.safeword-project/tickets/completed/115-stop-hook-improvements`
- **117** — Review Anthropic research for Safeword-relevant design insights (cancelled, epic: —)
  → `.safeword-project/tickets/completed/117-anthropic-research-review`
- **118** — Pre-ticket prompt gap — understanding reminder before ticket exists (wontfix, epic: —)
  → `.safeword-project/tickets/completed/118-pre-ticket-prompt-gap`
- **120** — ESLint Config Override System (done, epic: —)
  → `.safeword-project/tickets/completed/120-eslint-config-override-system`
- **121** — Phase 3 Scenario Completeness Pipeline (done, epic: —)
  Give the agent a structured, research-backed method for determining scenario completeness at Phase 3, replacing the current "draft happy/failure/edge" approach.
  → `.safeword-project/tickets/completed/121-phase3-scenario-completeness`
- **124** — Derive phase state from ticket files, don't cache it (done, epic: —)
  Eliminate stale phase state in multi-session/multi-developer scenarios by reading phase directly from ticket.md instead of caching in session state.
  → `.safeword-project/tickets/completed/124-derive-phase-state`
- **124b** — Add verify phase — done means done (done, epic: —)
  Split the overloaded `done` phase into `verify` (run evidence gates) and `done` (ticket closed). Prevents agents from skipping /verify + /audit by jumping straight to done.
  → `.safeword-project/tickets/completed/124b-verify-phase-gate`
- **125** — TDD step detection fragility and quality review bypass (done, epic: —)
  Address two related weaknesses in the enforcement architecture: the implicit format contract between BDD skills and hook parsers, and the one-shot nature of stop hook quality reviews.
  → `.safeword-project/tickets/completed/125-tdd-step-fragility`
- **126** — Prompt Hook Reminder for Research Validation (done, epic: —)
  Add one conditional reminder to the prompt hook for a moderate-risk transition that doesn't warrant a structural gate.
  → `.safeword-project/tickets/completed/126-prompt-hook-reminders`
- **127** — Resolve Before Reporting Uncertainty (done, epic: —)
  Change the stop hook quality review from declaring uncertainty to resolving it first.
  → `.safeword-project/tickets/completed/127-resolve-before-reporting-uncertainty`
- **128** — Implement phase requires test-definitions.md gate (done, epic: —)
  → `.safeword-project/tickets/completed/128-implement-phase-requires-test-definitions`
- **130** — Improve learnings discoverability for Claude Code agents (done, epic: —)
  Make the `.safeword-project/learnings/` folder self-discoverable via a Karpathy-style LLM Wiki pattern (plain markdown + auto-maintained INDEX.md + SAFEWORD.md instruction), with no hard scaling ceiling and portable across any agent that reads CLAUDE.md / AGENTS.md-style configs.
  → `.safeword-project/tickets/completed/130-learnings-discoverability`
- **131** — Fix 11 ESLint errors in hooks.test.ts (done, epic: —)
  Clean up the remaining 11 ESLint errors in `packages/cli/tests/integration/hooks.test.ts`. Originally Part 4 of ticket #109 (which specified 19 errors — 8 were fixed in earlier sessions, 11 remain).
  → `.safeword-project/tickets/completed/131-hooks-test-lint-fixes`
- **132** — Fix: upgrade self-installs as workspace:\* in monorepos (done, epic: —)
  Prevent `computePackagesToInstall` from listing packages that are already provided by workspace members.
  → `.safeword-project/tickets/completed/132-upgrade-self-install-workspace`
- **134** — Refactor: consolidate duplicate getWorkspacePatterns in detect.ts (done, epic: —)
  Single source of truth for reading workspace patterns from package.json.
  → `.safeword-project/tickets/completed/134-consolidate-workspace-patterns`
- **136** — Root ESLint Config: Dogfood the configs.cli/relaxedTypes Presets (done, epic: —)
  → `.safeword-project/tickets/completed/136-root-eslint-config-dogfood-presets`
- **137** — Verify customer lint rule overrides survive safeword upgrades (BDD) (done, epic: —)
  Prove, per-language, that a customer's override in their own lint config file is honored by safeword's LLM hook AND is not touched by `safeword upgrade`.
  → `.safeword-project/tickets/completed/137-customer-override-survival`
- **138** — Unify customer override contract in LLM hook (supersedes #019) (done, epic: —)
  → `.safeword-project/tickets/completed/138-unify-customer-override-contract`
- **139** — Harden the unified customer override contract (follow-up to #138) (done, epic: —)
  → `.safeword-project/tickets/completed/139-harden-unified-override-contract`
- **140** — Fix 3 pre-existing TypeScript errors blocking clean tsc baseline (done, epic: —)
  Restore a clean `bunx tsc --noEmit` baseline so future verify passes don't have to triage these as out-of-scope.
  → `.safeword-project/tickets/completed/140-fix-pre-existing-typecheck-errors`
- **141** — File upstream Claude Code bug: parallel-worktree creation flips parent core.bare=true (cancelled, epic: —)
  Get Anthropic to fix a race in Claude Code's worktree creation that breaks git ops across all sibling worktrees.
  → `.safeword-project/tickets/completed/141-claude-code-worktree-bare-race`
- **142** — End-to-end integration test for `install` / `upgrade` against a temp project (done, epic: —)
  Add a CI-runnable integration test that exercises `safeword install` and `safeword upgrade` against a temp project directory, asserting the user-visible result. Catch the class of bug where unit-tested pieces (templates, reconcile) compose into broken output.
  → `.safeword-project/tickets/completed/142-install-upgrade-integration-test`
- **143** — Stop-hook binary terminal: CONFIDENT or BLOCKED (done, epic: —)
  Replace the Stop-hook quality-review prompt with a universal CONFIDENT/BLOCKED binary terminal so the agent commits to a verdict instead of dumping uncertainty lists on the user.
  → `.safeword-project/tickets/completed/143-stop-hook-binary-terminal`
- **144** — Safeword cursor-parity enforcement (manifest + pre-commit + slash command) (done, epic: —)
  Stop folklore-driven Cursor parity. Replace it with an explicit manifest of pairs and contracts, enforced at commit-time (pre-commit hook) and on-demand (project-local slash command). Both gates live in safeword's repo only and never reach customers.
  → `.safeword-project/tickets/completed/144-audit-cursor-parity-check`
- **145** — Schema `createIfMissing: false` is silently ignored by executor (done, epic: —)
  Make `executeTextPatch` honor the `createIfMissing: false` flag on `TextPatchDefinition`, or remove the flag and update the schema comment to match actual behavior. Pick whichever direction reflects the intended product behavior.
  → `.safeword-project/tickets/completed/145-claudemd-createifmissing-drift`
- **146** — /verify output shape: status / decisions / next-actions separation (done, epic: —)
  Restructure the `/verify` skill's report so it cleanly separates status facts, user-decision questions, and agent next-actions. Eliminate the dump-style "everything in one verdict" pattern.
  → `.safeword-project/tickets/completed/146-verify-output-shape`
- **147** — Skill-invocation phase-gate infrastructure (v1: /verify + /audit at done) (done, epic: —)
  Build a general phase-gate infrastructure so that when a ticket enters a gated phase, the agent must have invoked the required skills in the current session. v1 enforces /verify and /audit at the done phase; future gates added by config.
  → `.safeword-project/tickets/completed/147-verify-audit-forcing-function`
- **147** — Worktree-clean dev loop: import ESLint presets from source (completed, epic: —)
  Make a fresh git worktree commit-able after one `bun install` — no pre-build step, no `--no-verify`.
  → `.safeword-project/tickets/completed/147-worktree-clean-dev-loop`
- **149** — Derive safeword-managed path list from SAFEWORD_SCHEMA at build time (done, epic: —)
  Replace the hardcoded `safewordPaths` array in `session-auto-upgrade.ts` with a build-time-derived list sourced from `SAFEWORD_SCHEMA.ownedFiles`, so it can't drift from the schema's source of truth.
  → `.safeword-project/tickets/completed/149-safeword-paths-from-schema`
- **150** — Revisit "Claude-only" Skill Classification (pending, epic: —)
  Decide whether the `CLAUDE_ONLY_SKILLS` set (`brainstorm`, `tdd-review`, `elicit`) reflects a principled boundary or historical under-investment in Cursor parity. If principled, document the rule. If historical, add Cursor rules for the affected skills.
  → `.safeword-project/tickets/completed/150-claude-only-skill-classification`
- **151** — PreToolUse hook: reset core.bare before git commands (open, epic: —)
  Defend ad-hoc git ops (status, mv, push) from Claude Code's parallel-worktree `core.bare = true` race, not just `git commit`.
  → `.safeword-project/tickets/completed/151-pre-tool-git-bare-fix`
- **154** — Task: Strip dead `version` field from .safeword/config.json (done, epic: —)
  Remove a write-once / never-read `version` field from `.safeword/config.json` that has caused a reasoning-LLM to flag projects as "stale" (real incident in `arcade-deep-research`, v0.25.14 → v0.32.1 upgrade). Plaintext `.safeword/version` is the live source of truth; the JSON field is vestigial state.
  → `.safeword-project/tickets/completed/154-strip-dead-version-from-config`
- **156** — Vendored-ignores export + install-time guidance + hygiene pass (done, epic: —)
  Give downstream safeword users a one-line escape hatch from "my strict ESLint config flags 84 errors inside `.safeword/**`," and harden the three actual code smells in vendored hooks while we're in there.
  → `.safeword-project/tickets/completed/156-vendored-ignores-export`
- **157** — Install-time auto-patch of consumer's ESLint config (done, epic: —)
  Make the 156 print-nudge moot for the common case. Most users won't read printed instructions and many don't know what ESLint is — so safeword should just add the line itself, with a backup, and tell the user it did. Print-nudge becomes the fallback for legacy configs and unrecognized patterns rather than the primary experience.
  → `.safeword-project/tickets/completed/157-install-time-auto-patch`
- **158** — Crockford Base32 ticket IDs, folder-name-equals-ID (done, epic: —)
  Eliminate ticket-ID collisions between parallel sessions AND across git branches by minting uncoordinated 6-char Crockford Base32 IDs and making the folder name the ID alone, so any ID collision becomes a real git merge conflict instead of silent corruption.
  → `.safeword-project/tickets/completed/158-crockford-ticket-ids`
- **163** — Fix `@vitest/eslint-plugin` missing-package failures in eslint-config tests (done, epic: —)
  Three test files in the eslint-configs area fail because `@vitest/eslint-plugin` isn't installed despite being declared in package.json. Get them green.
  → `.safeword-project/tickets/completed/163-vitest-eslint-plugin-missing`
- **164** — Fix broken strip-version migration in safeword upgrade (done, epic: —)
  Main's ticket 154 implementation didn't actually strip the `version` field — the test added to verify it fails on `origin/main`. Make it work.
  → `.safeword-project/tickets/completed/164-upgrade-strip-version-broken`
- **165** — Add pretest rebuild guard against stale dist/cli.js (done, epic: —)
  Always rebuild before tests so `runCli`-based tests can't silently pass/fail against a stale binary.
  → `.safeword-project/tickets/completed/165-pretest-rebuild-guard`
- **4N5Y28** — Replace novelResearchReminder boolean with monotonic per-file arrays (done, epic: —)
  Replace the mutable `novelResearchReminder?: boolean` in `QualityState` with two append-only string arrays (`learningsNudgesPending`, `learningsNudgesAcknowledged`) so the novel-claim nudge has per-file fingerprints, monotonic state transitions, and the disqualification message can name which files triggered.
  → `.safeword-project/tickets/completed/4N5Y28`
- **645W8H** — session-reentry-brief (done, epic: —)
  Make worktree re-entry fast — when a user returns to a Claude Code worktree after hours/days away, they should know "what was happening" and "what to do next" in under 10 seconds, without reading more than one or two lines.
  → `.safeword-project/tickets/completed/645W8H`
- **65TVGM** — Add mechanical-multi-file calibration example (done, epic: —)
  Add one calibration example to SAFEWORD.md's sizing-rule section that closes the apparent contradiction between "3+ files → feature" and the BDD skill's "Do NOT use for bug fixes / small isolated changes."
  → `.safeword-project/tickets/completed/65TVGM`
- **8CMXNG** — Per-pattern dedup for recentFailures (done, epic: —)
  Bound `state.recentFailures` by distinct pattern count via per-pattern dedup with timestamp refresh, so the array can't grow unboundedly on repeated firings of the same pattern.
  → `.safeword-project/tickets/completed/8CMXNG`
- **CXXB3P** — ticket-folder-legibility (done, epic: —)
  Make ticket folders self-describing on disk so `ls` tells you what each ticket is about without opening `ticket.md`.
  → `.safeword-project/tickets/completed/CXXB3P-ticket-folder-legibility`
- **E79QAC** — Rephrase novel-claim block to prevent misreading (done, epic: —)
  Rephrase the novel-claim disqualification message in `quality.ts:108` so it accurately describes the gate (flag-shown-to-user check) instead of the misleading "/quality-review first" phrasing.
  → `.safeword-project/tickets/completed/E79QAC`
- **J7VBGJ** — TDD ledger: SHA-or-skip on RED/GREEN/REFACTOR checkboxes (incl. cross-scenario refactor) (done, epic: —)
  Close the silent-skip gap in safeword's TDD enforcement by anchoring every per-scenario TDD step (and the feature-level cross-scenario refactor pass) to either a real git commit SHA or a deliberate `skip: <reason>` line. Done gate verifies the anchors.
  → `.safeword-project/tickets/completed/J7VBGJ`
- **MKVNFB** — Skip-with-reason for the dimensions.md gate (done, epic: —)
  Give the dimensions phase a deliberate escape valve — `skip: <reason>` as the entire file content — instead of forcing agents to produce a real dimensions.md when they have one obvious behavioral dimension and no real partitioning to do.
  → `.safeword-project/tickets/completed/MKVNFB`
- **XV72DT** — Warn when learning files contain verification stamps (done, epic: —)
  Prevent agents from fabricating "✅ Verified" claims that land in `.safeword-project/learnings/` and poison future-session context.
  → `.safeword-project/tickets/completed/XV72DT`
- **ZM32AK** — Clear pre-existing lint debt (done, epic: —)
  Drive `bun run lint` to zero errors so verify-gate Lint check goes green, with no behavior changes.
  → `.safeword-project/tickets/completed/ZM32AK`
