# HRN1Z6 — Verify Checklist

**Lint:** ✅ `eslint . && bun run --cwd packages/cli typecheck` exits 0 with zero output (no new violations, no typecheck errors)
**Install:** ✅ `bun install` resolved `eslint-plugin-jsdoc@63.0.0` cleanly (12 packages installed, 1 removed)
**Build:** ✅ Implicit via prepare hook during install (tsup ESM + DTS, no errors)
**Tests:** Not re-run — this is a dependency bump that only affects the lint pipeline. No source code touched; no behavior changes. Full suite was last green at K7N2QM verify (2110/2110, commit `c75703d1`); no source paths since then.

## Done-when criteria — evidence

| Criterion (from frontmatter)                                                  | Evidence                                                                                                  |
| ----------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------- |
| `packages/cli/package.json` shows `^63.0.0` in `dependencies`                 | `packages/cli/package.json:66` updated; commit `cdae6bd8`                                                 |
| Root `package.json` shows `^63.0.0` in `devDependencies` (not `dependencies`) | Root `package.json:29` added under `devDependencies`; old `dependencies` block (empty after move) removed |
| Lockfile reflects the new resolved version                                    | `bun.lock` updated; `bun install` resolved `eslint-plugin-jsdoc@63.0.0`                                   |
| `bun run lint` exits 0 with zero new violations                               | Verified — full lint pass clean                                                                           |
| K7N2QM W001 closeable as "closed in HRN1Z6"                                   | This ticket addresses that audit finding directly; commit message references it                           |

## Implementation surface

3 files touched, 1 commit:

- `packages/cli/package.json` (+1 / -1) — version bump only
- `package.json` (+1 / -3) — version bump + recategorization to `devDependencies` + removal of empty `dependencies` block
- `bun.lock` — regenerated by `bun install`

## Risk surface I considered

- **`require-throws` rule fix:** Safeword's two constructors (`SlugError`, `TicketIdCollisionError`) only call `super(...)` — no explicit `throw` statements. The v63 tightening doesn't apply. Lint pass confirmed.
- **Node 20 drop:** Safeword pins `node >=22` (`packages/cli/package.json:29`). The breaking change is already met.
- **Config-name compatibility:** `pluginJsdoc.configs['flat/recommended-error']` (consumed at `recommended.ts:31`) not renamed in v63. Import resolution clean.
- **Hoisting behavior:** Bun workspace hoisting unchanged. Moving the root entry from `dependencies` to `devDependencies` is a semantic-only edit; both root `node_modules/eslint-plugin-jsdoc/` and `packages/cli/node_modules/eslint-plugin-jsdoc/` still resolve correctly (verified via successful lint run).

**Next:** mark HRN1Z6 done (no audit needed for a single-dep task with full lint pass as the verification — audit is overkill for this scope).
