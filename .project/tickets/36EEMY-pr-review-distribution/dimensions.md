skip: the solution dimensions for this runner were explored and settled on the parent epic before this child was opened — `../WAWQA6-autonomous-pr-review/architect-brief.md` (§3 what we're building, §5 why not CodeRabbit/Greptile/Bugbot, §6 the honest risk list, §7 what we're NOT doing) and the epic ticket.md decision record. The architecture call this child inherits (vendor-agnostic HEADLESS `codex exec`/`claude -p` rather than claude-code-action; read-as-data in an unprivileged job handing off to a no-approve poster) is recorded with its rejected alternatives in `impl-plan.md` → Decisions, and is emitted as an ADR at implement exit. Re-deriving dimensions here would duplicate the epic, not inform the build.
