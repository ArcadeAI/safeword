{
  "feature": "features/architecture-narrative-blindspots.feature",
  "scenarios": {
    "A configured non-root narrative gets the nudge when a ticket moves the shape": ["packages/cli/tests/hooks/architecture-document-nudge.test.ts", "configured non-root narrative → nudges on movement, naming the configured path"],
    "A configured decision-record directory counts as a narrative": ["packages/cli/tests/hooks/architecture-document-nudge.test.ts", "configured decision-record directory → counts as a narrative, nudges on movement"],
    "A configured narrative that is missing on disk draws no advisory": ["packages/cli/tests/hooks/architecture-document-nudge.test.ts", "configured target missing, no root file → no nudge"],
    "An explicit configuration wins over a present root file even when its target is missing": ["packages/cli/tests/hooks/architecture-document-nudge.test.ts", "explicit configuration wins over a present root file even when its target is missing"],
    "An unconfigured host with a root ARCHITECTURE.md still nudges on shape movement": ["packages/cli/tests/hooks/architecture-document-nudge.test.ts", "new/removed module → fingerprint moved vs base → nudges, naming ARCHITECTURE.md"],
    "An unconfigured host with no narrative anywhere stays silent": ["packages/cli/tests/hooks/architecture-document-nudge.test.ts", "no narrative anywhere → no nudge (audit create-from-template path owns it)"],
    "An unchanged fingerprint stays silent even with a configured narrative": ["packages/cli/tests/hooks/architecture-document-nudge.test.ts", "unchanged fingerprint stays silent even with a configured narrative"],
    "An unparseable config falls back to the root ARCHITECTURE.md": ["packages/cli/tests/hooks/architecture-document-nudge.test.ts", "unparseable config falls back to the root ARCHITECTURE.md"],
    "An empty-string paths.architecture behaves as unconfigured": ["packages/cli/tests/hooks/architecture-document-nudge.test.ts", "empty-string paths.architecture behaves as unconfigured (root fallback)"],
    "A configured narrative is named in the advisory text": ["packages/cli/tests/hooks/architecture-document-nudge.test.ts", "names the resolved narrative in the advisory text"],
    "The root fallback is named as ARCHITECTURE.md": ["packages/cli/tests/hooks/architecture-document-nudge.test.ts", "new/removed module → fingerprint moved vs base → nudges, naming ARCHITECTURE.md"],
    "The architecture review prompt resolves the narrative via paths.architecture": ["packages/cli/tests/templates/architecture-narrative-prose.test.ts", "directs the agent to the paths.architecture narrative with root fallback"],
    "The audit skill's structural-drift check resolves the narrative via paths.architecture": ["packages/cli/tests/templates/architecture-narrative-prose.test.ts", "resolves the narrative via paths.architecture in the ARCHITECTURE.md checks"]
  }
}
