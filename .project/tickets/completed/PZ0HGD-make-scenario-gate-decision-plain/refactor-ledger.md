# Refactor Ledger: Make the scenario gate decision plain

- [x] Replace the whole-document `/saturat/` ban with precise assertions against the retired `Scenario saturation` and `Coverage saturation` decision headings and the terse `Saturated at` prompt.
- [x] Preserve the two positive plain-language question assertions as the behavioral contract.
- [x] Confirm repeated guide content is intentional generated/schema-owned delivery parity, not actionable duplication.
- [x] Regenerate the native Claude and Codex plugin surfaces from the canonical guide; retain required inventory and identity digest changes.

The ledger is complete; no refactor was deferred.
