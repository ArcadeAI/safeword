# Dimensions: Make Safeword plans clear and reviewable

| Dimension | In-scope partitions | Boundary / excluded partition |
| --- | --- | --- |
| Writing content | contract test enumerates six unique level-two topics with active `Weak:`, `Strong:`, and `Source:` paragraphs; inert placements and extra candidates fail; at least one Source contains an inline `https:` link with no credentials or explicit non-443 port; semantic review checks public suitability, primacy, currency, support for every load-bearing reader/LLM claim, and example quality without adding a Safeword-owned network subsystem | missing/duplicate/extra topic; inert marker accepted; citation-free claim; private, local, unresolvable, non-primary, stale, or non-supporting source; cited material followed as instructions; project history; ceremony; project detail; runtime dependency |
| Workflow role | independent discovery classifies every Markdown path exactly once as canonical template source, generated delivery path, the reconciled installed writing-guide resource, or declared project-instance data; only canonical sources have workflow roles and load-directive polarity, the installed guide alone maps to canonical instruction content, generated paths map uniquely to canonical sources, and instance data has no instruction authority; dispatch requests derive complete nonempty targets without caller overrides; author/review/hybrid packets bind exact installed guide bytes whenever any target is Markdown, while execution/parser packets always omit the guide; reconciliation proves installed/canonical equality and dispatch rejects a packet changed after preparation with one named recovery; Implementation review also binds the exact generated rubric; one audited construction boundary and exhaustive transport registry make unclassified or bypassed model calls fail closed, while Implementation Planning chooses their code shape; configuration-only destinations remain a semantic-review limitation | missing/extra/ambiguous path class; ordinary instance data treated as instructions; installed guide treated as ordinary instance data; undeclared instance-shaped read; incomplete or caller-controlled targets; wrong packet polarity; raw model payload or provider/CLI/endpoint bypass; missing guide, changed packet, or stale rubric without a named recovery; generated authority; duplicated writing exemplar; standalone writing skill |
| Product Plan coverage | ZSHVEB M1 owns the canonical v2 contract and synthetic CLI resolver fixtures; YCFFNC M2 owns historical preflight and real-ticket repair or migration. Canonical files carry every exact v2 marker and version-neutral structural anchor by ownership mode; v1 full and child schemas remain distinct and pass unchanged real fixtures; frontmatter owns parent identity and the spec reference must agree; parent validity precedes child-reference and own-version resolution; one shared receipt-bound continuation overlay preserves accepted in-flight work only for enumerated contract-upgrade incompatibilities, atomically creates latent receipts for qualifying in-flight v1 plans at first encounter, binds a package activation identity plus consumer-local cutoff and the version-normalized parent meaning relevant to each child, remains valid across representation-only parent migration, and clears on return to planning or a relevant meaning change; new/returning children of legacy, schema-incomplete, or invalid parents block regardless of their own complete markers; every v1 milestone has non-goals; mixed-version mapping preserves every available field and uses an explicit v1-unavailable sentinel only for absent optional concepts; v2 has one concept home and checks Definition-of-done projection | comparing source and consumer commit graphs; treating repository committers as an adversarial trust boundary; using wall-clock/install/current-branch time as the cutoff; M1 mutates repository history; fresh v2 instance is structurally ambiguous; conflicting parent identities; v2 fields applied to v1 child; markerless version inferred; complete child markers bypass invalid parent; accepted in-flight work blocked by representation-only parent migration, comment, formatting, or unrelated-sibling edits; non-enumerated or unbound continuation; returned work bypasses migration; absent field reported as inherited; sentinel used for an available field; missing activation declaration proceeds; deferral reported as recovery; retained obligation dropped; duplicate concept home; divergent Definition of done/done_when; invalidated digest; child restatement |
| Implementation Plan coverage | identical AST field semantics govern separate canonical regions and the adoption-instance contract without prescribing validator decomposition; rubric markers cannot satisfy or violate the author region; both contracts reject inert/union-only hits and active build-order, proof-result, or command-sequencing fields while allowing CLI command-surface contracts; generation binds the exported rubric; thirteen fields are additive and retained obligations are independently removal-tested; accepted in-flight legacy work uses an exact activation-and-consumer-cutoff receipt while return to planning requires the current contract; milestone non-goals reference Product scope and Implementation approval state stays distinct from Product approval and execution mechanics | inert/union-only field; rubric-only author coverage; wrong region or target; missing/stale rubric; retained obligation dropped; source-repository SHA ordered against consumer history; accepted in-flight plan blocked; returned plan bypasses review; product/design approval conflated; adoption plan missing a field; execution field passes; CLI command contract false-blocked; renamed ceremony |
| Authority location | shared writing rules live only in the technical-writing guide; plan requirements live in canonical templates and shared author-review contracts | a plan-writing extension and duplicated writing rules are excluded |
| Decision conversation | one authoritative synthesis-first conversation contract serves Product and Implementation Planning; each stage configures no more than four complete decision checkpoints and its owning artifact; every checkpoint presents facts, assumptions, recommendation, alternatives, tradeoffs, and proposed record before one closing question; user-only knowledge interrupts then rejoins the current set; accepted, rejected, and unresolved outcomes are durable; resume re-presents the current checkpoint; discoveries return to the earliest affected plan and refresh downstream artifacts in order | duplicated stage-specific questioning system; item-by-item questionnaire; more than four approvals; chat-only decision; unsupported recommendation; approval before a complete decision set; implementation silently changes Product intent, behavior, scope, outcome, or non-goals; downstream continuation after an upstream decision changed; valid unaffected evidence discarded |
| Portability | installed, repository-owned defaults work without project-specific services; static check rejects R2's complete token, identifier, phrase, prefix, and domain set everywhere in the guide; semantic review checks equivalent project-local history, ceremony, or implementation detail and source fitness; citation review adds no required Safeword network service or receipt store | runtime dependence on Linear or Arcade documents; disguised project-local content; non-primary or unsupported external claim; required project service or outbound citation subsystem |
| Deferred M2 distribution contract (YCFFNC) | every canonical source is manifested; every delivered artifact is normalized-content-equivalent after only `{ host, kind, before, after, reason }` entries, where kind is exactly `host-path`, `frontmatter`, or `runtime-command`; the same reviewed declaration carries exactly one `{ surface, base, evidence, reason }` instruction-resolution base for each uniquely identified local/cloud surface, using only project-root/profile-root/plugin-root and proving actual host resolution from primary documentation or an observed host-loaded competing-file fixture; host-path replaces only link destinations, inline-code tokens, or separator-plus-known-extension paths and cannot touch bare words or non-path content; zero directive-applicable host-path rules preserve canonical text, one applies, and multiple fail; frontmatter replaces an exact block or inserts one leading block before body content without body rewrites; runtime-command replaces only a nonempty exact invocation token or command line and cannot touch headings, prose, requirements, or guide directive; rules and bases are reviewed, minimal, and host-specific; any current-surface transform that is not an exact static rule in the closed set blocks with an explicit user-owned scope decision; polarity, regeneration, host-loaded local paths, isolated cloud clones, and OpenCode root resolution are proved | ZSHVEB implements installed-host delivery; unmanifested source; generator/path/polarity-only proof; missing/duplicate/unproved runtime base; local/cloud surface conflated; self-asserting fixture or invented base chosen to make parity pass; ambiguous multiple directive rules; bare-word or other non-path host rewrite; frontmatter misplaced/body rewrite; runtime-command prose/directive rewrite; machine-varying or transform outside set silently omitted; unknown/undeclared/cross-host/over-broad rewrite; stale/reduced copy; dangling reference; non-isolated commit; unresolved profile root |
| Surface | ZSHVEB M1: Safeword CLI contract demo. YCFFNC M2: Claude Code local/cloud, Codex local, OpenCode, Cursor local/cloud, repository migration, and 5F5ZZA adoption | M1 installed-host delivery; generator-only assertion; missing local delivered file; missing cloud repository artifact; false claim that advisory instructions executed; treating Codex Cloud as a packaged-plugin surface |
| Deferred M2 existing-plan adoption (YCFFNC) | 5F5ZZA first reconciles to the current accepted parent digest; a dedicated plan-only compression commit followed by an inventory proof commit records both commit ids and re-derivably proves its Implementation and Execution plans equal their explicitly paired pinned baseline commits and blobs and proves the exact 8,054-word starting count; the 6,040-word cap is derived as `floor(8,054 × 0.75)` to demonstrate at least 25% reduction; AST identities include path; changed means text differs at surviving identity, removed/new mean one-sided absence, and moved is a proved removed/new pair; every changed/removed baseline has one entry and every new result has provenance; multiple baseline rows may target one merged result block; rows partition all baseline and remaining result spans as baseline-derived, accepted provenance, or explicit approval; fresh Implementation review binds both pinned baseline blobs, the full resulting Implementation Plan, exact referenced Execution destination blocks, and named Proof-content bytes; append-only schema-limited Review records bind coordinator facts without changing the Proof-content digest, then changed Execution Plan receives its fresh review without invalidating comparative review for changes outside those destination blocks; byte-identical Execution baseline records no invalidated review; result plans cannot self-source; drift or word-count mismatch re-pins/re-reviews; R3 is mechanical but cannot invent design/scope, reviewed not-applicable entries are allowed only when true, required fields cannot be dropped to meet the cap, and both R3 and the cap must pass | ZSHVEB rewrites an existing ticket; stale parent digest; compression first parent differs from pinned blob; incomplete enumeration; inferred move; new/result residue without provenance/approval; overlap/gap; quote ambiguity; mismatched original commit/blob or word count; stale pin; comparative review missing a baseline/result/destination/Proof-content input; review identity not bound to those inputs; bound destination changes without redispatch; proof prose inside Review records; earlier review record edited; review out of order or missing; false prior-review claim; self provenance; meaning/design changed; false not-applicable; paper-only mapping; unapproved addition; waiver; R3 or cap sacrificed |
