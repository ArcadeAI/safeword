# Refactor ledger

- [x] Assert that the composed root scripts exist before asserting the aggregate command.
- [x] Match README command lines without coupling the test to alignment spaces.
- [x] Model the root and `packages/cli` README blocks explicitly so command-scope regressions fail the real-file contract.
- [x] Clarify that the documented acceptance lane must run from the root, where feature discovery is complete.
