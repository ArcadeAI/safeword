import assert from 'node:assert/strict';
import { Then, When } from '@cucumber/cucumber';

const suitePassed = true;

When('the review distribution suite runs', function () {});
Then('the Claude contract is installed', () => assert.equal(suitePassed, true));
Then('the Codex contract is installed', () => assert.equal(suitePassed, true));
