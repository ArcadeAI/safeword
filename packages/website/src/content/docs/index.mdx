---
title: The first coding agent discipline system
description: The first coding agent discipline system for Cursor and Claude Code
template: splash
hero:
  tagline: Your agent doesn't get to finish until the tests pass.
---

<a href="/getting-started/quick-start" class="splash-docs-link">
  Docs
</a>

<p class="hero-subtitle">
  <span>Safeword is the first coding agent discipline system for Cursor and Claude Code.</span>
  <br />
  <span class="hero-subtitle-detail">
    It runs alongside your existing stack — never modifies your application code or runtime
    dependencies.
  </span>
</p>

---

## The Problem

Your agent ships fast. But it skips tests, ignores lint, and pushes whatever "looks right."

Your senior engineers stop trusting it. Your vibe-coders can't tell working code from confident garbage.

<p class="soul">AI doesn't decide when it's done. You do.</p>

---

## The Fix

**Automatic Enforcement** — Every edit gets linted. Every session ends with review. Your agent can't skip it.

**Strict Quality** — Code, architecture, docs, dependencies. Nothing slips through.

**Structured Discipline** — TDD enforced. Debugging structured. Refactoring gated.

**Human-friendly** — Guardrails only fire during AI sessions. Your workflow stays the same. Use the linting in CI or precommit hooks if you want — your choice.

---

## Install

```bash
bunx safeword@latest setup
```

Works with **Cursor** and **Claude Code**. Auto-detects **TypeScript**, **Python**, **Go**, **Rust**.

---

## What Happens

Safeword adds configuration files, hooks, and skills to your project — it never modifies your application code. Commits to your repo — your team gets discipline the moment they pull.

---

## Stay Current

```bash
bunx safeword@latest upgrade
```

---

## Reference

<nav class="ref-links">

[Quick Start](/getting-started/quick-start)
[CLI](/reference/cli)
[Hooks & Skills](/reference/hooks-and-skills)
[Configuration](/reference/configuration)

</nav>

---

<footer class="splash-footer">

[GitHub](https://github.com/TheMostlyGreat/safeword) · MIT License · © {new Date().getFullYear()}

</footer>

<script is:inline>{`
  (function() {
    var lineHeight = 0;
    var lineOffset = '76px';
    var line = null;

    function calculateDimensions() {
      var footer = document.querySelector('.splash-footer');
      if (!footer) return;

      var el = footer, top = 0;
      while (el) { top += el.offsetTop; el = el.offsetParent; }
      lineHeight = top + footer.offsetHeight;

      var style = getComputedStyle(document.documentElement);
      lineOffset = style.getPropertyValue('--sw-line-offset') || '76px';
    }

    function createLine() {
      if (line) line.remove();
      calculateDimensions();

      line = document.createElement('div');
      line.id = 'sw-vertical-line';
      document.documentElement.appendChild(line);
      updateLine();
    }

    function updateLine() {
      if (!line) return;
      var scrollY = window.scrollY || window.pageYOffset;
      var viewportHeight = window.innerHeight;
      var visibleEnd = Math.min(lineHeight - scrollY, viewportHeight);

      if (visibleEnd <= 0) {
        line.style.display = 'none';
      } else {
        line.style.cssText = 'position:fixed;top:0;left:' + lineOffset + ';width:4px;height:' + visibleEnd + 'px;background:#000;pointer-events:none;z-index:9999;';
      }
    }

    createLine();
    window.addEventListener('resize', createLine);
    window.addEventListener('scroll', updateLine);
  })();
`}</script>
