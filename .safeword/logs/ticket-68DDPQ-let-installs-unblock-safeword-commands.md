# Work Log: Let dependency installs unblock Safeword commands

**Anchored to:** `.project/tickets/68DDPQ-let-installs-unblock-safeword-commands/ticket.md`

---

## Session: 2026-08-05

- [06:10] Created the task from GitHub issue #1763 and recorded its tracker link.
- [06:10] Revalidated the current templates: `pre-tool-dependency-readiness.ts` denies a stale compound command whenever any segment is dependency-backed. A recovery such as `pnpm install --frozen-lockfile && pnpm exec safeword doctor --json` is therefore blocked before the install can run; the post-tool marker cannot self-heal it.
- [06:10] Next: compare a narrow safe compound-command exemption with keeping remediation as a separate call.
- [06:12] Figure-it-out — Frame: decide how a PreToolUse gate can permit its documented recovery plus retry without executing a dependency-backed command after a failed or concurrent recovery.
- [06:12] Options: (A) allow any command containing an install segment; (B) allow only a leading recognized install or exact `touch node_modules` recovery followed exclusively by `&&` segments; (C) preserve separate recovery and retry calls.
- [06:12] Research domains: hook lifecycle (PreToolUse decides once; PostToolUse cannot prevent or re-run a completed command), Bash list semantics (`&&` executes the next command only after success, unlike `||`, `;`, and pipelines), current parser boundaries, and package-install classification. Claude's current hook reference and GNU Bash documentation support the lifecycle and sequencing constraints.
- [06:12] Decision: choose B. A denies unsafe `||`, `;`, and pipe chains while removing no capability beyond the separately allowed `touch node_modules` recovery. A allows the retry after a failed install; C keeps a proven zero-work block. Premortem: a partial shell parser could misclassify complex syntax; accept only a leading recognized recovery and an all-`&&` chain, and deny unsupported forms.
- [06:37] TDD: added a failing stale-readiness test for `bun ci && bun run test`; after implementing the narrow recovery classifier, the focused hook suite passed 94/94. Added the corresponding `touch node_modules && …` acceptance case and `||`, `;`, and pipe denial cases.
- [06:37] Synced the canonical template changes into the dogfood install and Claude plugin runtime. Confirmed template/plugin parity and the plugin release inventory contract.
- [06:37] Independent quality review completed without findings. Focused tests, `bun run typecheck`, Prettier, parity, and plugin release checks passed. The generated full test plan completed, but its final buffered output was unavailable; the ticket remains in verification pending a definitive full-suite/CI result.
- [19:49] Caught the work up to `origin/main`, revalidated issue #1763 against the current hook, and synchronized the template, dogfood, and Claude-plugin runtime copies. The rebase retained the narrow all-`&&` recovery boundary.
- [19:49] Re-ran the quality-review, scoped audit, lint, typecheck, parity, and plugin-release lanes. Audit reported no violations across 9 modules and 7 dependencies; explicit post-rebase typecheck, lint, parity, plugin contract, and diff hygiene checks passed. The generated full plan completed but its final output remains unavailable locally, so CI is the aggregate authority.
- [19:51] Re-ran the focused hook-process suite after rebase: 94/94 tests passed. Byte-compared the template, dogfood, and Claude-plugin runtime hook copies; all match.
- [19:56] Opened draft PR #1992 for CI. The ready-PR lint gate requires `status: done`; kept the ticket in verify/in-progress and issue #1763 open while the full matrix runs.
- [23:10] Quality-review round 2 with an independent reviewer briefed to refute the invariant rather than confirm it. Round 1 had reasoned about the operators the tokenizer *reports* and never enumerated what it leaves *inside* a segment; two ways to run a guarded command against unready dependencies survived, both reproduced against the real hook process.
- [23:10] CRITICAL 1: the exemption sat after the `ready`/`unsupported` early exit, so `touch node_modules && <retry>` was exempt while readiness was `missing`. With no `node_modules`, `touch` creates an empty regular *file*, exits 0, the retry runs with nothing installed, readiness stays `missing` forever, and the stray file blocks the real install. `formatDependencyRecovery` only ever offers that recovery for `stale`. Gated the touch branch to `stale`.
- [23:10] CRITICAL 2: `parseShellCommandList` does not split on a single `&`, so `bun ci && start & run` looked like an all-`&&` chain while bash runs `(bun ci && start) &` then `run` immediately and unconditionally. Added `hasBackgroundOperator` to the shared tokenizer (redirection and quoted/escaped `&` excluded) and made the classifier reject any segment carrying one.
- [23:10] Accepted without change: only the leading segment is classified, so `bun ci && rm -rf node_modules && bun run test` still passes — the gate stops accidents, not deliberate self-sabotage. Documented in the docstring.
- [23:10] Out of scope, worth filing: `isInstallSegment` counts installs that never materialize `node_modules` (`--production`, `--omit=dev`, `yarn --mode=update-lockfile`), and the shared tokenizer's `&` blindness also affects the kill and ledger gates.
- [23:10] Re-verified: 141/141 focused hook tests, full suite 6621 passed, typecheck, lint, prettier, parity (241 pairs, 8 contracts), and plugin release contract at 0.73.0. The 7 sandbox failures are chmod-based permission simulations that cannot fail under uid 0.
- [04:04] Re-read all PR #1992 comments: no submitted review, no unresolved review thread. The two earlier critical fixes remain present. Resolved the two deferred concerns directly: the shared tokenizer now emits unquoted `&` as a list boundary for every gate, and the shared install classifier rejects known partial/no-link forms for both recovery and post-tool stamping.
- [04:04] Figure-it-out chose the shared, literal, fail-closed implementation over a dependency-only exception or a full Bash parser. Regression coverage now includes dependency recovery, Cursor routing, process kills, ledger writes, architecture staging, and file-descriptor redirections.
- [04:04] Full verification passed: `bun run test` reported Retro Relay 167 passed/1 skipped and CLI 6782 passed/5 skipped. Full lint, formatting, parity (241 pairs, 8 contracts), plugin contract, and diff hygiene passed. Dependency audit has 0 errors and one pre-existing no-orphans warning for unchanged `packages/cli/src/codex-plugin/hooks.ts`.
- [04:22] Fetched the newly advanced main (`cbd2700`, Bun generator pin) before push. The retro issue remains open and relevant. Rebasing replayed the three ticket commits and required only generated plugin inventory digest resolutions; full verification passed again with the same 167/1 and 6782/5 results.
- [04:43] Main advanced once more with the independent-review-agent fallback (`7414594`). Rebased the ticket commits a second time, resolving only generated inventory digest conflicts. The exact final head passed full verification: Retro Relay 167 passed/1 skipped and CLI 6784 passed/5 skipped; lint, parity, plugin contract, formatting, diff hygiene, and the 0-error dependency audit also pass.
