# 36PD6T — Implementation log

## 2026-07-22

- Root cause: the configured Codex dispatcher loads the packaged canonical bridge, while the installed recorder reads the dogfood resolver. The in-progress dogfood resolver expects an ordered `entries` cache but the canonical bridge still writes the legacy single-entry shape, so valid proof is discarded as missing identity.
- Test scope: use real Codex/Cursor adapter-to-installed-helper integration tests because the defect is at the package-dispatch/cache/recorder boundary; add parser/cache unit coverage only for the ordered queue edge cases.
- First slice: add a failing Codex packaged-dispatch regression using the documented relative helper command, then synchronize the canonical cache representation and adapter parsing with dogfood.
- RED observed: the real packaged Codex dispatcher records the first `verify` receipt, then the second `audit` helper reports `no run identity`. This is the expected single-entry cache overwrite/consumption failure.
- Implementation: moved the bridge to an ordered per-runtime queue in the shared template helper, removed the duplicate legacy writer in the Codex CLI dispatcher, and synchronized all three dogfood hook files.
- Additional finding: Cursor canonicalizes a macOS working directory from `/var/...` to `/private/var/...`; strict string comparison therefore rejected its own documented command. Existing absolute paths are now canonicalized before exact-root comparison, with the relative and `$PROJECT_DIR` forms still explicit allow-list entries.
- Green evidence: `bun run test tests/hooks/review-stamp-bridge.test.ts tests/integration/codex-cursor-skill-fallback.test.ts` (55 tests); `bun run lint`; and `bun run format:check` all pass.
- Compatibility correction: the full packaged Codex parity BDD scenario also uses `$CLAUDE_PROJECT_DIR`. The bridge now recognizes that form only when the hook receives the same canonical active root; an inline or chained `PROJECT_DIR`/`CLAUDE_PROJECT_DIR` reassignment remains fail-closed. The queue-shaped bridge assertion was updated to match the intentional multi-receipt cache representation.
- Final focused evidence: 57 bridge tests, 83 Cucumber scenarios / 986 steps, package lint, root format check, TypeScript typecheck, `git diff --check`, and byte-identical template/dogfood hooks pass.
- Full-suite evidence: `bun run test` completed with 5,282 passed and 5 skipped but one unrelated failure in `tests/integration/golang-golden-path.test.ts` (`lint hook formats files without safeword config`). The Go formatter did not update `fallback.go` under locally installed golangci-lint 2.12.2; none of this ticket's files cover that hook. This blocks a clean feature done-gate, so the ticket remains in verify.
- Blocker resolved by SQ5KFS: missing optional Go config now uses the installed configless formatter without a networked Safeword upgrade. The project-generated verification plan passed 355 Vitest files / 5,284 tests (5 skipped), 484 BDD scenarios / 15,000 steps (3 and 4 skipped respectively), and TypeScript typecheck. The Codex bridge BDD assertion was factored into shared helpers to satisfy ESLint complexity; the exact scenario passes.
