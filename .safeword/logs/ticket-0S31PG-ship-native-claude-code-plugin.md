# Work Log: Ship native Claude Code plugin

**Anchored to:** `.project/tickets/0S31PG-ship-native-claude-code-plugin/ticket.md`

---

## Session: 2026-08-02

- [15:35] Adopted GitHub #1785 as feature ticket 0S31PG.
- [15:37] Revalidated current Claude plugin documentation and local CLI 2.1.170: plugins carry skills, commands, agents, hooks, executables, and cache-relative paths; install/list/marketplace operations have non-interactive commands; user/project/local scopes are observable; `/reload-plugins` activates changes in-session.
- [15:37] Isolated profile smoke: local marketplace add and user-scope install succeeded; `claude plugin list --json` reported exact version, enabled state, scope, and cache path. Moving the local marketplace source offline left the installed cache intact but surfaced a marketplace `cache-miss`, so the packaged smoke must distinguish cache execution from marketplace health.
- [15:38] Figure-it-out frame: choose the default install and migration lifecycle; a choice is wrong if it removes working protection before exact plugin execution is proven, overwrites user/third-party configuration, or makes cleanup mutate trust/install state.
- [15:38] Options: (1) project-first enablement in committed `.claude/settings.json`; strongest team discovery, but every teammate still fetches/trusts the external plugin and settings merging becomes migration-critical. (2) profile-first explicit install plus project-scoped proof and cleanup; cleanest lifecycle and matches Codex precedent, but team recommendation remains a separate opt-in. (3) replace legacy assets during ordinary setup/upgrade; smallest implementation, but violates continuity and cleanup separation.
- [15:38] Research domains: Claude install/update/cache/trust behavior; Safeword schema ownership and reconciliation; atomic and concurrent cleanup; cross-host parity; install/error UX; release/version contracts.
- [15:38] Recommendation: profile-first Expand -> Prove -> Contract because exact SessionStart proof, not installation metadata, is the load-bearing safety boundary. Project-first was close on team discovery but loses on ambiguous per-profile readiness and shared-settings mutation. Premortem: this fails if cache-version proof drifts from the manifest actually executing, so bind proof to plugin version plus hook-manifest digest and test update overlap. Next: confirm the JTBD set, then author Rules and engineering scope.
- [15:41] User confirmed the feature classification and JTBD set. Authored Rules covering fresh install, ownership preservation, cache/project-state split, idempotence, exact proof, safe failure, cleanup non-mutation, transaction recovery, generated release completeness, contract drift, host parity, and packaged smoke evidence.
- [15:43] User amended the criteria: changes should be immediately reflected in the live session/task. Current Claude docs confirm `/reload-plugins` applies installed/enabled/disabled components without restart. Added TBU1.R5 plus a safe fallback: if reload is refused or does not provide an exact proof boundary, report it and retain legacy authority.
- [15:45] User invoked /figure-it-out for the engineering-scope decision. Frame: choose how install, live activation, exact execution proof, and cleanup compose; wrong means either unnecessary restarts or retiring protection based only on installation metadata.
- [15:45] Options to test: (1) SessionStart-only proof after profile install; safest precedent but no live-task activation. (2) SessionStart plus post-reload event proof, where the next supported live event records exact plugin identity; meets live-task goal if the host exposes a reliable event. (3) trust `claude plugin list --json` as activation proof; smallest but observes configuration rather than execution.
- [15:45] Investigation plan: Claude reload/events — identify a reliable event after `/reload-plugins`; scopes/trust — choose user vs project default without hidden shared mutation; cache identity — bind proof to immutable installed content; shared runtime ownership — determine what Claude cleanup can retire without breaking Cursor; transaction safety — reuse or generalize fingerprinted cleanup/recovery; release/parity — inventory every generated and versioned surface; recovery UX — make every non-ready state self-remediating.
- [15:57] Cold-start verdict: insufficient. A fresh reader could not plan lifecycle API/state, coexistence authority, recognized legacy identity, runtime/package runner, exact asset boundary, setup/upgrade behavior, parity exceptions, or automated/manual proof boundaries. Appended each gap to Open Questions as required.
- [15:59] Host proof: isolated user-scope install, repeat install, disable/enable, marketplace update, plugin update, list JSON, and `--init-only` all succeeded without touching the real profile. A Git-marketplace install executed SessionStart from the versioned plugin cache. Removing only the marketplace source `plugin/` directory left cached hook execution working, while `plugin list --json` correctly reported source health degradation; removing marketplace metadata entirely prevented load. The smoke must preserve marketplace metadata while removing source fixtures.
- [15:59] Figure-it-out verdict: recommend user-scoped install + direct bundled Bun runtime + dual SessionStart/UserPromptSubmit proof. SessionStart-only was close on migration simplicity but loses the requested live-task activation; plugin-list proof loses because it observes configuration, not execution. Premortem: the design fails if coexistence lets old and new functional hooks run together, so plugin dispatch must be proof-only while a viable legacy event remains and cleanup must be fingerprinted/atomic. Next: confirm the ticket's Scope / Out of Scope / Done When contract, then author scenarios.
- [16:02] User confirmed the final Scope / Out of Scope / Done When contract. Transitioned intake -> define-behavior; dimensions precede executable scenarios.
- [16:11] Independent quality review requested changes: the feature lacked a successful public install path plus explicit declined-cleanup, read-only-status, and plugin-mode reconciliation safeguards. Added those boundaries, canonical cache-path proof, an unsupported-host state with tested 2.1.170 baseline, and duplicate skill/command rejection.
- [16:19] Fresh re-review found install itself lacked the unsupported-host mutation guard. Added a pre-mutation rejection for older or unparseable Claude hosts and clarified that 2.1.170 is Safeword's tested floor, not an Anthropic-declared minimum.
- [16:22] Final independent quality review approved: currency and primary-source grounding current; correct/elegant/no-bloat; no critical issues or suggestions. Transitioned define-behavior -> scenario-gate for formal review-spec validation.
- [16:36] Scenario gate converged after three fresh adversarial passes. Added complete status precedence, per-event and post-cleanup authority, deterministic recovery, runtime identity/cache-path rejection, symlink/path containment, ordinary-upgrade preservation, install convergence, destructive-command proof rejection, full profile immutability, historical fingerprints, damaged-cache smoke, and independent manual boundaries. Final tally: 0 must-fix, 0 should-strengthen, 8 looks-good; 40 feature groups match 40 ledger rows. Review stamp recorded; transitioned scenario-gate -> plan-implementation.
