# Work Log: 2C1E82 — Make each agent's plugin fully self-contained

## 2026-09-02 — Proof-fidelity repair

- Re-read the completed epic after the user requested the suggested BDD/TDD quality changes.
- Confirmed the branch is current with `origin/main` (0 behind, 88 ahead) before editing.
- Reopened the ticket at `implement`; runtime behavior remains in scope only if a real boundary test exposes a defect.
- Repair targets: replace adjacent/static proof mappings with discriminating entry-point tests, make outline examples change the exercised fixture, correct the missing-authored-input claim, remove unrelated ticket-lineage behavior, and refresh ledger evidence after rebase.
- RED: the proof manifest named the missing real-entry-point tests and `bdd-proof-tags.test.ts` failed on the first absent declaration. The repository-root wrapper was separately blocked by three unrelated retro-relay timeouts; the package-local proof command isolated the intended failure.
- GREEN: real generated/installed workflow tests now cover Codex legacy-runtime precedence, unavailable package failure, Claude/OpenCode quality-review dispatch, Cursor audit execution, and all four state-writing authorities. Real lifecycle apply tests cover Codex-only profile removal while Cursor remains and mixed-authority reconciliation.
- Product gaps found by the stronger tests: direct Claude/Cursor helper calls skipped ignore creation; Codex plugin audit lookup assumed `dist/` rather than `runtime/`; uninstalling one native profile could remove shared project enrollment while Cursor remained. Fixed each at its narrow shared boundary and regenerated the native plugin artifacts.
- Focused lane: 103/103 tests passed across proof provenance, workflow execution, state initialization, lifecycle reconciliation, OpenCode identity, and schema isolation.
- Independent scenario re-review approved the repaired feature with five should-strengthen findings. The follow-up RED adds a zero-change audit partition, splits positive and negative enrollment behavior, makes the OpenCode package/legacy result distinguishable, moves profile lifecycle behavior under a builder-owned Rule, and documents why version-control status has no non-Git partition.
- GREEN: a real sourced Bash caller now proves the empty-diff export and remains available. The focused audit/proof lane passed 43/43, and the revised 34-scenario feature matches a reachable, collision-free R/G/R ledger exactly.
- The second independent scenario review caught a Rule-lineage defect introduced by the persona correction. The spec now declares NTB1.R2 for identity-owned profile lifecycle, SWM1.R1 owns release authority only, and every remaining warning was resolved through observable scenario language or an explicit legacy-cleanup boundary. The content-bound spec self-review stamp was written for this epic.
- The third independent scenario review approved the corrected lineage. Its remaining clarity suggestions were applied directly: Claude/OpenCode real-dispatch boundaries are explicit, mixed selection carries every varied host surface, and project-selection behavior no longer duplicates profile catalogue completeness.
- The final scenario review approved and exposed the last asymmetric proof boundary: Cursor's sourceable project helper lacked the failure and empty-diff cases now proven for Codex. The follow-up RED adds both Cursor cases and turns no-install/project-runtime claims into observable postconditions.
- GREEN: Cursor now has real sourced-shell success, error-return, and empty-diff tests through its installed project helper; plugin-backed workflows assert no project runtime materializes after execution. The audit/proof lane passed 45/45 after narrowing cross-host checks to executable payloads rather than legitimate host configuration directories.
- The terminal scenario review caught a real catalogue wording mismatch: the OpenCode plugin was present in the Given but omitted from the installed-set Then. The feature now names the exact plugin/command/agent/skill/reference set and closes the remaining host-boundary, fallback-marker, surface-tag, and enrollment-precondition ambiguities.
