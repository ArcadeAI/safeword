# Work Log: 2C1E82 — Make each agent's plugin fully self-contained

## Final integration closeout — 2026-09-03T02:32:02Z

- Main `258b1055e` is integrated without unresolved conflicts. The unchanged writable-PATH recheck passed both cases; all 55 release checks passed. Independent merged-runtime review `bbc02ed5-6a0d-4ac2-965b-1811d08b572d`, 37 reviewer scenarios, typechecks, audit, compatibility checks, and the packaged-runtime walkthrough are complete. All six children and 38 epic scenarios remain complete; no cross-scenario refactor is warranted and no accepted epic behavior is uncovered.
- Marked the epic done after these local gates. PR #3623 remains Draft; final merge-commit CI and the current-head seven-gate readiness record are tracked in the PR body. No Ready promotion, merge, release, or verified Desktop activation is claimed. Earlier failed aggregates and surrounding reviewer-fixture limitations remain explicit below.

## 2026-09-02 — Proof-fidelity repair

- Re-read the completed epic after the user requested the suggested BDD/TDD quality changes.
- Confirmed the branch is current with `origin/main` (0 behind, 88 ahead) before editing.
- Reopened the ticket at `implement`; runtime behavior remains in scope only if a real boundary test exposes a defect.
- Repair targets: replace adjacent/static proof mappings with discriminating entry-point tests, make outline examples change the exercised fixture, correct the missing-authored-input claim, remove unrelated ticket-lineage behavior, and refresh ledger evidence after rebase.
- RED: the proof manifest named the missing real-entry-point tests and `bdd-proof-tags.test.ts` failed on the first absent declaration. The repository-root wrapper was separately blocked by three unrelated retro-relay timeouts; the package-local proof command isolated the intended failure.
- GREEN: real generated/installed workflow tests now cover Codex legacy-runtime precedence, unavailable package failure, Claude/OpenCode quality-review dispatch, Cursor audit execution, and all four state-writing authorities. Real lifecycle apply tests cover Codex-only profile removal while Cursor remains and mixed-authority reconciliation.
- Product gaps found by the stronger tests: direct Claude/Cursor helper calls skipped ignore creation; Codex plugin audit lookup assumed `dist/` rather than `runtime/`; uninstalling one native profile could remove shared project enrollment while Cursor remained. Fixed each at its narrow shared boundary and regenerated the native plugin artifacts.
- Focused lane: 103/103 tests passed across proof provenance, workflow execution, state initialization, lifecycle reconciliation, OpenCode identity, and schema isolation.
- Independent scenario re-review approved the repaired feature with five should-strengthen findings. The follow-up RED adds a zero-change audit partition, splits positive and negative enrollment behavior, makes the OpenCode package/legacy result distinguishable, moves profile lifecycle behavior under a builder-owned Rule, and documents why version-control status has no non-Git partition.
- GREEN: a real sourced Bash caller now proves the empty-diff export and remains available. The focused audit/proof lane passed 43/43, and the revised 34-scenario feature matches a reachable, collision-free R/G/R ledger exactly.
- The second independent scenario review caught a Rule-lineage defect introduced by the persona correction. The spec now declares NTB1.R2 for identity-owned profile lifecycle, SWM1.R1 owns release authority only, and every remaining warning was resolved through observable scenario language or an explicit legacy-cleanup boundary. The content-bound spec self-review stamp was written for this epic.
- The third independent scenario review approved the corrected lineage. Its remaining clarity suggestions were applied directly: Claude/OpenCode real-dispatch boundaries are explicit, mixed selection carries every varied host surface, and project-selection behavior no longer duplicates profile catalogue completeness.
- The final scenario review approved and exposed the last asymmetric proof boundary: Cursor's sourceable project helper lacked the failure and empty-diff cases now proven for Codex. The follow-up RED adds both Cursor cases and turns no-install/project-runtime claims into observable postconditions.
- GREEN: Cursor now has real sourced-shell success, error-return, and empty-diff tests through its installed project helper; plugin-backed workflows assert no project runtime materializes after execution. The audit/proof lane passed 45/45 after narrowing cross-host checks to executable payloads rather than legitimate host configuration directories.
- The terminal scenario review caught a real catalogue wording mismatch: the OpenCode plugin was present in the Given but omitted from the installed-set Then. The feature now names the exact plugin/command/agent/skill/reference set and closes the remaining host-boundary, fallback-marker, surface-tag, and enrollment-precondition ambiguities.

## 2026-09-02 — Restarted-session verification and final review

- Merged `origin/main` at `f15c55f7a` (0.83.1), regenerated both plugins, and restored dependencies from the frozen lockfile without a project installation.
- Scenario review `5c52c7dc-47a6-4c25-b491-7b3060c8fd88` approved the 36-scenario feature. Remaining optional matrix expansions were bounded by `dimensions.md`; no further scenario review loop was started.
- Stronger tests found fenced-Markdown guard misses, direct-helper enrollment bypass, and false `changed` reporting when run identity is absent. Fixed those shared boundaries and strengthened the full Cursor catalogue proof.
- The isolated-cache generic-helper regression failed at `79617e9d9` and passed at `8d95856df`; 19/19 workflow/helper tests passed. The source checkout had masked the bad parent-directory lookup.
- Quality review `2149ad5f-b074-4845-954f-fd2a18370c24` requested removal of native command-shape false negatives. Six lower-level examples failed as expected. A manual packaged-CLI walk also exposed missing success text required by the generated skills; the four-host proof caught Codex and OpenCode failing that contract.
- The combined RED is `520ec3a83`. The fix removes about 50 lines of native parsing, uses direct unavailable-path rejection, and returns the skill's required confirmation through the CLI presentation contract. Focused verification passed 73/73 tests; supported lifecycle fixture capture passed 12/12, with only Cursor tree digests changing after the host-neutral verification example.
- Review warnings: preserve legacy cleanup under the accepted migration boundary; generated proof commands already pass the resolved project root explicitly; no update-check implementation exists on the audited CLI command route (the environment flag is a legacy fixture setting). Fixed the misplaced Cursor imports. Kept the two small layout resolvers local, and did not add speculative subprocess/error or partial-payload matrices. Partial legacy fixtures prove successful package execution despite missing legacy helpers, not a callable legacy precedence marker.
- Two full-verifier attempts were stopped after new concrete defects were found; neither is claimed as completed evidence. One authentication-fallback failure and one machine-contract failure did not reproduce in their focused suites. The final authoritative run must be collected after the review fixes.
- Independent review `3eb0bcf7-1b73-487d-8fe6-1c32840f6f7a` exposed the same runner allowlist hole on Cursor and enrollment removal when another native host remains. RED `dc52dd7c7` reproduced eight missed Cursor commands and actual loss of `.safeword/SAFEWORD.md` after Codex removal with Claude still installed.
- Removed Cursor's runner heuristic, allowing only the catalogue's specific cross-host data references. Lifecycle removal now consults existing registry observations for remaining native profiles and retains shared project substrate without adding a second ownership registry. Tests cover Claude, Codex, OpenCode, and Cursor retention; legacy runtime preservation remains intact.
- Unselected-profile observation exposed ambient OpenCode state in lifecycle fixtures. Mocked that external profile boundary explicitly, preserving the original single-host fixture expectations rather than updating snapshots to absorb machine-dependent behavior.
- Addressed review warnings with byte-exact audit stdout and the documented absolute Cursor proof command. Kept no-identity/non-enrollment CLI behavior non-blocking as already specified; identity edge cases and missing-Bun diagnostic enrichment are optional follow-ups, not reasons to widen this repair.
- The 37-scenario independent gate `382eaf82-6afc-4053-937f-bf97ea31a8b7` approved with no errors. Its optional clarity suggestions do not alter the accepted contract; the recommended OpenCode diagnostic rewrite would incorrectly imply the legacy hook executed, so it was not applied. Lower-level shared-state contracts remain paired with real four-host wiring proof.
- Full verification completed the first CLI pass with 8959 passing, 13 skipped, and five failures in plan/remove wiring. Direct preview reproduced ambient-profile retention; empty profile roots restored the intended fixture. Tracing `--agents=none` against the current selection implementation and README/FAQ also exposed a real compatibility error: explicit project removal must remain available despite installed profiles. Added RED `cc5d1cb9e`, restricted retention to partial host removal, and added the explicit-removal BDD counterpart. The two earlier code reviews became stale while these evidenced repairs changed their inputs; no stale verdict is treated as approval.
- Scenario gate `a77603b7-0153-44f0-907e-4cef2a675801` approved all 38 scenarios with no errors; recorded the cross-agent Claude Opus phase stamp. Optional warnings were answered against the executable proofs: shared-state cases run the real CLI and are paired with four-host wiring; no-install checks inspect output and absence of runtime files; the OpenCode unsupported-output diagnostic is an exact packaged-hook contract, not any crash; identity inventory is an inspectable artifact with exact digest-set assertions; enrollment is explicitly marker-based; catalogue success is a CLI release check, not live host activation. No additional feature or matrix expansion is needed.
- Full verification finished red: 8959 CLI tests passed, five planning tests failed, and root acceptance had one failing user-scoped Claude scenario. Node 24 CI reproduced that same acceptance failure. Corrected its expected no-op (no user Claude profile exists; Codex retains enrollment); focused acceptance now passes 1 scenario / 46 steps. Isolated planning profiles rather than weakening destructive-plan assertions. The explicit-removal fixture now establishes both installed profiles before asserting preservation.
- Independent code review `5c7fa297-48e9-4efc-bf7a-e04141e015a9` approved with no errors. Took three worthwhile warnings: exercise Cursor's actual installed catalogue, reject executable descendants/traversal under exempted data paths, and remove selected legacy Claude payload while retaining Cursor. RED `3cd6fae58` reproduced both guard misses and retained legacy files (3 failures / 17 passes). The latter tests strengthen existing authority/selection rules; they do not expand the accepted feature matrix.
- Remaining review warnings are deliberately bounded: the origin/main digest is the selected-schema compatibility contract, while migration suites own stale-runtime cleanup; its explicit fixture-update mode is developer tooling, not an attacker boundary. Host-provided identity syntax is trusted and malformed-key hardening is outside this repair. Shared immutable-by-convention effects, attempted-vs-selected reporting on preflight failure, and the intentional CLI/helper non-enrollment exit distinction do not weaken the accepted runtime contract. No extra abstraction or optional hardening was added.
- GREEN `17819b917`: all 97 focused authority/lifecycle/planning/provenance/compatibility tests passed; complete lint/typecheck and 865 pre-push schema checks passed. Audit found no dependency violations across 417 modules / 691 dependencies; the existing isolated Python experiment limitations remain explicit. All 38 ledger scenarios are complete; entered verify on 2026-09-02T23:02:51Z.
- Follow-up independent review `4ba1237f-6da0-4400-a9f2-eb2240682de7` (Claude Opus, cross-agent) approved with no errors. Remaining non-blocking findings were answered without expanding this repair: obsolete shared legacy files remain under the declared migration-cleanup boundary; quote-concatenated traversal is additional trusted-catalogue hardening rather than supported shipped workflow behavior; absent project-profile labels, alias replacement prose, and architecture-baseline differential assertions belong to adjacent unified-command scenarios, whose changed user-scope case already has exact no-op assertions. The ordinary-help step is supplied by the broader acceptance support, not required to be duplicated in this bounded file. Exact data-expression/template coupling is intentional and covered by the actual installed Cursor catalogue. No further optional review loop is warranted.
- CI run `33692911053` passed on `09b9f9421`: both Node jobs green; Node 24 recorded 8972 CLI tests passed / 9 skipped, 1496 acceptance scenarios / 68600 steps passed, physical-install proof passed, 39 proof registrations passed, and 55 release checks passed. The local verifier completed both full CLI passes (8968 passed / 13 skipped each), both relay/collector passes, builds, typechecks, and dependency scans. Its root acceptance lane failed eight reviewer-process cases under concurrent local workload; the result remains red, not completion evidence. All 15 focused cases covering those failures then passed unchanged in 14.984 seconds (681 steps), consistent with their short real-subprocess/probe deadlines being sensitive to local load. Re-running the complete root/package acceptance lanes without altering production timeouts.
- The unchanged full root retry passed 1493 scenarios / 3 skips plus 39 proof checks. Package acceptance reproduced four classification failures: 588 scenarios passed, four failed because an unsupported or invalid-output reviewer timed out during probing before reaching the intended behavior. Source inspection confirmed that probes and answers share the attempt deadline; the fixture still defaulted to two seconds while the normal probe ceiling is five. The earlier repair funded only two cases, leaving the shared fixture inconsistent. Replaced those special cases with one five-second fixture default, a thirty-second run bound, and a forty-second Cucumber step bound. Production deadlines and the explicit three-second deadline assertion remain unchanged. Both root and package TypeScript checks passed. Cucumber's configured `paths` are additive, so the subsequent file-targeted command is exercising the complete package acceptance set; its final count, not its label, determines the evidence.
- The consistent fixture budget passed all 592 package scenarios and 11046 steps. Independent fixture review `8a0b890c-496c-4b65-b57d-773dffa230b9` then exposed one non-discriminating input: an invalid `--help` model was mistaken for a capability probe before the fake logged the launch. Changed only that input to `invalid model`, which cannot take the probe shortcut; an accidental alternate launch now breaks the existing output/launch assertions. Both grammar scenarios passed (38 steps). Also bounded the CLI process at 35 seconds, below the 40-second step timeout. Final-file formatting, lint, and package TypeScript checks passed.
- Fixture-review warnings were bounded explicitly: the three-second deadline's elapsed-time assertion remains the accepted deadline proof; private reviewer-cache isolation is a separate hermeticity improvement and must respect executable trust ancestry; existing Given-step double execution, dead step definitions, and PID-diagnostic polish do not affect this repair's asserted outcomes. No extra abstractions or production timeout changes were added. Moved the one orphaned fake-reviewer directory from the intentionally interrupted diagnostic run to `/tmp/safeword-runtime-walk.6FEpCH/interrupted-fixture-codex-RXWKUI`; no user files were deleted.
- The final full package retry completed with 591 passing scenarios and one three-second deadline case expiring during probing (`probe_timed_out` rather than the expected post-launch `timed_out`); retained that failed run. Package proof provenance passed 39/39 separately. The explicit deadline/launch assertion is unchanged; this is a local startup-timing limitation, not an accepted relaxation of the deadline contract.
- Fixture review `e1e1d681-ff31-4f80-83ae-8eb124b1caf0` found a second non-discriminating proof: prefix greps could call roles separate while the context file also appeared in the target array. The fixture now captures actual reviewer input and checks exact membership of both arrays for primary and alternate routes. The first implementation used a new fixture environment variable that the production allowlist correctly stripped; switched to the existing `SAFEWORD_REVIEW_PROMPT_LOG` channel without changing production permissions. The in-flight review of that first draft cannot approve the revised file.
- A disposable Cucumber hook injected the reviewer's exact role-leak counterexample after real CLI execution. Both role assertions failed specifically on the extra `supporting-evidence.md` target (2 expected failures / 39 passing steps); no production code was mutated. An earlier diagnostic overlapped the proof wrapper's `dist/` rebuild and failed before execution, so it was discarded as infrastructure failure and rerun only after the build completed. The counterexample hook lives only under `/tmp`, not in the delivered test suite.
- Final fixture verification passed all 37 reviewer scenarios / 699 steps, including the strict three-second deadline, with the counterexample removed. Final formatting, lint, and package TypeScript checks passed; both package/root proof entry points have 39 passing checks. Review `f1175b4f-a99d-4f10-bf25-cf064ce8297e` returned stale after the capture-channel correction and was not reused. Fresh cross-agent Claude Opus review `485a1b8c-1dff-4364-a252-986c809bd363` approved the final file with no errors.
- Final optional findings do not require expanding this repair: descendant-poll timeout alignment, reviewer-cache isolation/umask, and root-proof permission fixtures are test-hygiene follow-ups; the supported macOS/Ubuntu runs passed their current cases. Kept the strict deadline assertion and reported the observed local-load limit instead of weakening it. No scenario combines config-writing Givens, so speculative config merging is unnecessary here. Active scenarios already exercise alternate-model funding; unused definitions, a redundant help-argument check, hypothetical double-run launch counting, Alpine tool paths, and a deliberately synthetic leak sentinel do not leave the accepted behavior unproven.
- 2026-09-03T01:38:14Z: all six children and all 38 epic scenarios are complete. Verify, audit, and quality-review current-run receipts were recorded successfully. Marked the epic done; PR #3623 remains Draft, with final-head CI tracked in its readiness record. No Ready promotion, merge, release, or verified Desktop-hook activation is claimed.
- Commit `e130f285a` passed CI run `33704538993`: Node 22 and Node 24, package tests, physical install, acceptance, release gates, lint, conformance, parity, and audit were green. Node 24 recorded 8972 CLI tests passed / 9 skipped, 187 relay tests, 106 collector tests, one physical-install test, and 55 release tests. The downloaded acceptance log omitted its terminal summary, so no new scenario count is inferred from that log; the job's terminal success is the recorded evidence.
- Main advanced during CI to `258b1055e` (PR #3617: OpenCode fallback and ranked/scoped review routes), while GitHub's PR response briefly retained the old base SHA. Fetch and merge-tree confirmed real generated-file conflicts. Merged main without committing yet; regenerated both native bundles and the lifecycle fixtures instead of selecting either generated side. The 12-case recapture passed. Relative to `e130f285a`, only Cursor install/check/upgrade tree digests changed for the imported hook changes; all result digests and other fixture contracts remained identical. Reopened verify for combined-tree evidence; prior approvals and CI remain historical evidence, not proof of the integration.
- Combined-tree static verification passed root and package TypeScript. Diff audit found no dependency violations across 475 modules / 886 dependencies and no config drift; the known GEPA callback/import-linter limits remain. The repository-wide principle scan reported seven baseline dead references in two unrelated, unchanged plans (`CKWE2D`, `3F5Z6P`); the explicit `2C1E82` plan trace passed. No unrelated ticket was edited to silence that baseline.
- Repeated the packaged Codex first-use walkthrough against the newly generated combined runtime in `/tmp/safeword-merge-walk.xdr0tz`: missing state was created, repeated calls retained one `/skill-invocations.log` ignore rule, the authored note stayed unchanged, no executable project runtime appeared, and plain output printed `[skill-invocation-log] verify ✓`. Only the existing enrollment marker, authored note, exact ignore file, and invocation log were present.
- The focused integration run's first attempt timed out waiting for the shared test lock; no test was started or counted. Queued a longer bounded wait behind the canonical full suite in e31a and coordinated without interrupting it.
- The combined-tree integration lane completed with 535 passes, two skips, and one reviewer capability-probe timeout in the writable-PATH case; its positive trust/launch assertion was never reached. Retained the failed aggregate and queued an unchanged targeted recheck. All lifecycle compatibility checks passed without fixture-update mode.
- Fresh fixture review `32a289fb-4912-4be4-b8d2-db1dbd092020` found that the credential-emitting fake exited successfully, bypassing the nonzero-exit diagnostic path described by its Given. Made it exit 7 and required the real result's `fallback_failure` to be `process_failed` before checking that stdout/stderr contain no raw output or credential. All 37 reviewer scenarios / 699 steps passed on the combined runtime. A disposable hook restored the old exit-zero fixture; the scenario failed specifically on `invalid_output` versus `process_failed`, proving the new assertion detects the old false-green path. No counterexample hook ships. Final-file formatting, lint, and TypeScript passed.
- Final merged-runtime review `bbc02ed5-6a0d-4ac2-965b-1811d08b572d` approved (Claude Opus, cross-agent, no errors). Remaining warnings are not silently counted as resolved: the adjacent contract-writing Cucumber scenario can fail earlier in packet preparation, while `review/contract-file-failure.test.ts` directly exercises the contract boundary and asserts exact redacted message plus `process_failed`; the late-answer scenario needs an emission marker before it can independently prove that race. These are surrounding reviewer-feature proof improvements, not gaps in this epic's 38 authority/state/selection scenarios. Retained the explicit deadline assertion and its documented local-load sensitivity. User-config/cache isolation, interrupted-run fixture cleanup, default-model wording, and unreachable/redundant steps remain bounded fixture-hygiene follow-ups. The paired credential rejection now supplies the strong leak oracle, so duplicating it in the explanatory positive scenario adds no required coverage. The fixed one-megabyte rejection boundary is deliberately independent of the implementation constant; deriving the test size from that constant would stop detecting an accidental limit change. No additional production scope or abstraction was added.
